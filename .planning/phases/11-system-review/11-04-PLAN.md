---
phase: 11-system-review
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - AUDIT.md
autonomous: false
requirements: [TWR-04, TWR-08]

must_haves:
  truths:
    - "AUDIT.md documents every production unwrap() call with safe/unsafe classification"
    - "AUDIT.md includes API surface review of MCP tools, HTTP endpoints, CLI commands, config keys"
    - "AUDIT.md catalogs test coverage gaps for follow-up phase"
    - "AUDIT.md lists all code quality fixes made in plans 01-02"
    - "All critical issues are fixed or documented; non-critical issues cataloged"
  artifacts:
    - path: "AUDIT.md"
      provides: "Public audit report for open-source release"
      min_lines: 100
      contains: "unwrap"
  key_links:
    - from: "AUDIT.md"
      to: "crates/memcp-core/src/"
      via: "references specific files and line numbers"
      pattern: "crates/memcp-core"
---

<objective>
Produce a comprehensive AUDIT.md report covering: unwrap() audit, API surface review, test coverage gaps, code quality findings, and workspace->project rename verification. Includes a checkpoint for the user to review API surface findings before finalization.

Purpose: Build contributor trust by transparently documenting what was reviewed, what was fixed, and what remains. This is a public artifact for the open-source release.
Output: AUDIT.md in repo root — the definitive quality report.
</objective>

<execution_context>
@/Users/ayoamadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoamadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-system-review/11-CONTEXT.md
@.planning/phases/11-system-review/11-RESEARCH.md
@.planning/phases/11-system-review/11-01-SUMMARY.md
@.planning/phases/11-system-review/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conduct audits and produce AUDIT.md</name>
  <files>AUDIT.md</files>
  <action>
    Conduct four audit activities and write all findings into a single AUDIT.md file in the repo root.

    **Audit 1: Production unwrap() inventory (TWR-04)**
    Read each file with production unwrap() calls (not test files). For each:
    - File and line number
    - Context (what is being unwrapped)
    - Classification: SAFE (with justification) or NEEDS FIX (with recommended fix)
    - Known locations from research:
      - `pipeline/curation/algorithmic.rs:19-28` — 10x LazyLock Regex::new().unwrap() — SAFE (compile-time string literal patterns, standard Rust idiom, panics at startup not runtime)
      - `transport/server.rs:35` — Mutex lock unwrap — SAFE (standard Rust idiom; poisoned Mutex indicates unrecoverable thread panic)
      - `pipeline/temporal/mod.rs:557` — evaluate context
      - `pipeline/chunking/splitter.rs:141-142` — .first()/.last().unwrap() on non-empty Vecs — evaluate safety
      - `pipeline/enrichment/worker.rs:183, mod.rs:296` — regex/JSON schema init — evaluate
    - Scan all other production .rs files for any additional unwrap() calls missed by initial research

    **Audit 2: API surface review**
    Catalog every public interface:
    - **MCP tools:** Read transport/server.rs for all `#[tool]` functions. List name, parameters, description.
    - **HTTP endpoints:** Read transport/api/*.rs for all route handlers. List method, path, parameters.
    - **CLI commands:** Read main.rs (crates/memcp/src/main.rs) for all Clap subcommands. List command, flags, description.
    - **Config keys:** Read config.rs for all config struct fields. List key, type, default, env override.
    - Flag any naming inconsistencies (e.g., snake_case vs kebab-case, abbreviated vs full names).
    - Verify workspace->project rename is complete: confirm --project flag (not --workspace) is primary, serde aliases kept for backward compat. Note: per research and CONTEXT.md, keeping `alias = "workspace"` is fine for launch.
    - If any additional renames are needed beyond workspace->project, flag them with [REVIEW NEEDED] for user decision.

    **Audit 3: Test coverage gap analysis**
    - Count tests per module (grep for `#[test]` and `#[tokio::test]` in each subdirectory).
    - Identify modules with zero or minimal test coverage.
    - Identify recently-added modules (10.1 load_test, 11.1 provenance, 11.2 trust/curation) and their test coverage.
    - Catalog gaps — do NOT write tests (per user decision, deferred to follow-up phase).
    - Follow Rust best practices: public API should be tested, internal helpers less critical.

    **Audit 4: Code quality summary**
    Summarize what was fixed in Plans 01 and 02 (read their SUMMARYs):
    - Clippy error fixed (logic bug in cli.rs)
    - N clippy warnings fixed (by category)
    - Stale features removed
    - Locomo test resolved
    - logging.rs TODO resolved

    **AUDIT.md structure (Claude's discretion on exact format):**
    ```
    # memcp Code Audit — v0.1.0 Pre-Release

    **Date:** 2026-03-09
    **Scope:** Full codebase (106 .rs files, ~35k LOC) including Phases 10.1, 11.1, 11.2

    ## Executive Summary
    [3-5 sentences: what was reviewed, key findings, overall health]

    ## Code Quality Fixes
    [Summary of clippy fixes, stale features, dead code removal]

    ## Production unwrap() Audit
    [Table: file, line, context, classification, justification]

    ## API Surface Review
    ### MCP Tools
    [Table: name, parameters, notes]
    ### HTTP Endpoints
    [Table: method, path, parameters, notes]
    ### CLI Commands
    [Table: command, flags, notes]
    ### Configuration Keys
    [Table: key, type, default, notes]
    ### Naming Consistency
    [Findings, any flagged renames]

    ## Test Coverage Analysis
    [Table: module, test count, coverage assessment, gap notes]
    [List of specific gaps for follow-up phase]

    ## Cataloged Issues (Non-Critical)
    [Table: issue, location, severity, recommendation]
    [Items deferred to future phases]

    ## Workspace to Project Rename Status
    [Verification that rename is complete across all surfaces]
    ```
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && test -f AUDIT.md && grep -q "unwrap" AUDIT.md && grep -q "API Surface" AUDIT.md && grep -q "Test Coverage" AUDIT.md && echo "PASS"</automated>
  </verify>
  <done>AUDIT.md exists with all four audit sections: unwrap inventory, API surface review, test coverage gaps, code quality summary</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review AUDIT.md findings</name>
  <files>AUDIT.md</files>
  <action>
    Present AUDIT.md to user for review. Key areas requiring human judgment:
    1. unwrap() safe/unsafe classifications — confirm agreement
    2. API naming consistency findings — decide on any flagged renames
    3. Test coverage gaps — confirm catalog is complete, nothing needs immediate fixing
    4. Any [REVIEW NEEDED] items from API surface review
    If user requests changes, update AUDIT.md accordingly.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && test -f AUDIT.md && echo "PASS"</automated>
  </verify>
  <done>User has reviewed and approved AUDIT.md findings; any requested changes applied</done>
</task>

</tasks>

<verification>
- AUDIT.md exists in repo root with all required sections
- Every production unwrap() is documented with classification
- API surface is fully cataloged (MCP, HTTP, CLI, config)
- Test coverage gaps identified by module
- User has reviewed and approved findings
</verification>

<success_criteria>
- AUDIT.md is comprehensive, accurate, and publication-ready
- All production unwrap() calls audited and classified
- API surface fully documented with naming consistency check
- Test coverage gaps cataloged (not fixed — follow-up phase)
- workspace->project rename verified complete
- User approved the audit findings
</success_criteria>

<output>
After completion, create `.planning/phases/11-system-review/11-04-SUMMARY.md`
</output>
