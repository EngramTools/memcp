---
phase: 11-system-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/memcp-core/src/cli.rs
  - crates/memcp-core/Cargo.toml
  - crates/memcp-core/src/benchmark/locomo/dataset.rs
  - crates/memcp-core/src/logging.rs
autonomous: true
requirements: [TWR-01, TWR-03, TWR-05, TWR-06]

must_haves:
  truths:
    - "cargo clippy no longer emits the logic bug error on cli.rs:898"
    - "Stale feature flags wave0_07_5 and wave0_07_7 are removed from Cargo.toml"
    - "Locomo dataset test either passes or is marked #[ignore] with justification"
    - "logging.rs TODO is resolved (either implemented or explicitly deferred with doc comment)"
    - "workspace→project rename is verified complete: --project is the primary CLI flag, serde alias retained for backward compat, MCP tool params use project not workspace"
  artifacts:
    - path: "crates/memcp-core/src/cli.rs"
      provides: "Logic bug fix at line 898"
      contains: "format_memory_json(&h.memory, true)"
    - path: "crates/memcp-core/Cargo.toml"
      provides: "Clean feature flags"
    - path: "crates/memcp-core/src/logging.rs"
      provides: "Resolved TODO"
  key_links:
    - from: "crates/memcp-core/Cargo.toml"
      to: "all crate consumers"
      via: "feature flag removal"
      pattern: "no wave0_ features"
---

<objective>
Fix targeted code quality issues blocking compilation and clean build: clippy hard error, stale feature flags, failing test, logging TODO, and workspace→project rename verification.

Purpose: Unblock Plan 02 (bulk clippy warning fixes require compilation) and establish a clean baseline for the system review.
Output: Compilable crate with no clippy errors, no stale features, no failing tests, resolved TODO, verified rename.
</objective>

<execution_context>
@/Users/ayoamadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoamadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-system-review/11-CONTEXT.md
@.planning/phases/11-system-review/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix clippy error + remove stale feature flags</name>
  <files>crates/memcp-core/src/cli.rs, crates/memcp-core/Cargo.toml</files>
  <action>
    1. In `crates/memcp-core/src/cli.rs` line 898: change `format_memory_json(&h.memory, verbose || true)` to `format_memory_json(&h.memory, true)`. The `verbose || true` is always `true` — this is a clippy hard error (logic bug). The intent was clearly to always pass `true` in the JSON output path (the non-JSON path below handles verbose separately).

    2. In `crates/memcp-core/Cargo.toml`: remove the two stale feature flag lines:
       - `wave0_07_5 = []` (line 15)
       - `wave0_07_7 = []` (line 17)
       These features are not referenced anywhere in code (verified via grep). They were from Phase 07.5/07.7 wave iterations and are dead code.

    3. Verify no code references these features: `grep -rn "wave0_07_5\|wave0_07_7" crates/ --include="*.rs"` should return nothing.

    4. Run `cargo build` to confirm compilation succeeds after the error fix.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo build 2>&1 | grep -c "error" | xargs test 0 -eq</automated>
  </verify>
  <done>cli.rs logic bug fixed (verbose || true → true), stale features removed, crate compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Fix locomo test + resolve logging TODO</name>
  <files>crates/memcp-core/src/benchmark/locomo/dataset.rs, crates/memcp-core/src/logging.rs</files>
  <action>
    1. **Locomo dataset test** (`benchmark::locomo::dataset::tests::test_load_locomo_dataset_valid`):
       - The test tries to load an external LoCoMo dataset file that isn't included in the repo.
       - Mark the test with `#[ignore = "requires external LoCoMo dataset file — download separately, see benchmark docs"]`.
       - This is the cleanest resolution: the test isn't broken, it just requires external data.
       - Verify: `cargo test benchmark::locomo` should show the test as ignored (not failed).

    2. **logging.rs TODO** (line 86):
       - The TODO says "Add file output layer if config.log_file is set". Currently it just warns and uses stderr.
       - Resolution: Replace the TODO comment with a proper doc comment explaining the current state and that file logging is deferred to a future phase. Change the comment block to:
         ```rust
         // File-based logging is not yet implemented. When log_file is configured,
         // we warn and fall back to stderr. Implementation planned for a future phase.
         ```
       - Keep the existing `tracing::warn!()` that notifies the user at runtime.
       - This resolves the TODO without adding scope — the user decision says "resolve TODO" and research recommends deferring implementation.

    3. Run `cargo test -- --test-threads=1` to verify no test regressions (the locomo test should show as ignored).
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo test benchmark::locomo 2>&1 | grep -E "ignored|ok"</automated>
  </verify>
  <done>Locomo test marked #[ignore] with justification, logging.rs TODO replaced with explicit deferral comment, all other tests still pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify workspace→project rename completeness</name>
  <files>crates/memcp-core/src/cli.rs</files>
  <action>
    The workspace→project rename was done in Phase 08.8. This task verifies completeness across all surfaces and documents the status for the audit (Plan 11-04).

    Run the following verification checks:

    1. **CLI flags:** `grep -rn "workspace" crates/memcp/src/main.rs crates/memcp-core/src/cli.rs` — confirm `--project` is the primary flag and no `--workspace` flag exists. The `resolve_project()` function should check `MEMCP_PROJECT` env var with `MEMCP_WORKSPACE` as a fallback.

    2. **MCP tool params:** `grep -rn "workspace" crates/memcp-core/src/transport/server.rs` — confirm all tool parameter names use `project`, not `workspace`. If any `workspace` references remain in tool schemas, rename them to `project`.

    3. **HTTP API params:** `grep -rn "workspace" crates/memcp-core/src/transport/api/` — confirm query/body params use `project`. Fix any remaining `workspace` references.

    4. **Config keys:** `grep -n "workspace" crates/memcp-core/src/config.rs` — confirm primary config key is `project` with `alias = "workspace"` for backward compat. The serde alias is intentionally kept per CONTEXT.md decision ("keeping alias = workspace is fine for launch").

    5. **DB column:** `grep -rn "workspace" migrations/` — confirm migration 008 renamed the column. Verify current schema uses `project`.

    6. **Storage layer:** `grep -rn "workspace" crates/memcp-core/src/storage/` — confirm all SQL queries reference `project` column.

    If any surface still uses `workspace` as the primary name (not as a backward-compat alias), fix it. Document findings as a checklist for inclusion in AUDIT.md (Plan 11-04 Task 1).
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && ! grep -rn "\-\-workspace" crates/memcp/src/main.rs && echo "No --workspace CLI flag found: PASS"</automated>
  </verify>
  <done>workspace→project rename verified complete across CLI, MCP, HTTP, config, DB, and storage. Serde alias retained for backward compat. Findings documented for AUDIT.md.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with no errors
- `cargo test -- --test-threads=1` passes (locomo test shows as ignored, not failed)
- `grep -rn "wave0_07" crates/memcp-core/Cargo.toml` returns nothing
- `grep -n "TODO" crates/memcp-core/src/logging.rs` returns nothing
- No `--workspace` CLI flag in main.rs
</verification>

<success_criteria>
- Zero compilation errors from cargo clippy
- Stale feature flags removed from Cargo.toml
- Locomo dataset test ignored with justification
- logging.rs TODO resolved (deferred with clear doc comment)
- All existing tests pass
- workspace→project rename verified complete across all surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/11-system-review/11-01-SUMMARY.md`
</output>
