---
phase: 11-system-review
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - crates/memcp-core/src/pipeline/auto_store/filter.rs
  - crates/memcp-core/src/pipeline/auto_store/parser.rs
  - crates/memcp-core/src/pipeline/auto_store/watcher.rs
  - crates/memcp-core/src/pipeline/auto_store/mod.rs
  - crates/memcp-core/src/pipeline/consolidation/similarity.rs
  - crates/memcp-core/src/pipeline/consolidation/mod.rs
  - crates/memcp-core/src/pipeline/content_filter/regex_filter.rs
  - crates/memcp-core/src/pipeline/content_filter/semantic_filter.rs
  - crates/memcp-core/src/pipeline/content_filter/mod.rs
  - crates/memcp-core/src/pipeline/extraction/ollama.rs
  - crates/memcp-core/src/pipeline/extraction/openai.rs
  - crates/memcp-core/src/pipeline/extraction/pipeline.rs
  - crates/memcp-core/src/pipeline/extraction/mod.rs
  - crates/memcp-core/src/pipeline/gc/worker.rs
  - crates/memcp-core/src/pipeline/gc/dedup.rs
  - crates/memcp-core/src/pipeline/gc/mod.rs
  - crates/memcp-core/src/pipeline/summarization/ollama.rs
  - crates/memcp-core/src/pipeline/summarization/openai.rs
  - crates/memcp-core/src/pipeline/summarization/mod.rs
  - crates/memcp-core/src/pipeline/curation/mod.rs
  - crates/memcp-core/src/pipeline/curation/worker.rs
  - crates/memcp-core/src/pipeline/curation/algorithmic.rs
  - crates/memcp-core/src/intelligence/embedding/local.rs
  - crates/memcp-core/src/intelligence/embedding/openai.rs
  - crates/memcp-core/src/intelligence/embedding/pipeline.rs
  - crates/memcp-core/src/intelligence/query_intelligence/ollama.rs
  - crates/memcp-core/src/intelligence/query_intelligence/openai.rs
  - crates/memcp-core/src/intelligence/query_intelligence/temporal.rs
  - crates/memcp-core/src/intelligence/query_intelligence/mod.rs
  - crates/memcp-core/src/intelligence/recall/mod.rs
  - crates/memcp-core/src/intelligence/search/salience.rs
  - crates/memcp-core/src/storage/store/postgres.rs
  - crates/memcp-core/src/transport/api/delete.rs
  - crates/memcp-core/src/transport/api/search.rs
  - crates/memcp-core/src/transport/daemon.rs
  - crates/memcp-core/src/transport/server.rs
  - crates/memcp-core/src/transport/health/mod.rs
  - crates/memcp-core/src/import/batch.rs
  - crates/memcp-core/src/import/chatgpt.rs
  - crates/memcp-core/src/import/claude_ai.rs
  - crates/memcp-core/src/import/export/markdown.rs
  - crates/memcp-core/src/import/export/mod.rs
  - crates/memcp-core/src/import/markdown.rs
  - crates/memcp-core/src/import/openclaw.rs
  - crates/memcp-core/src/benchmark/dataset.rs
  - crates/memcp-core/src/benchmark/evaluate.rs
  - crates/memcp-core/src/benchmark/ingest.rs
  - crates/memcp-core/src/benchmark/prompts.rs
  - crates/memcp-core/src/benchmark/report.rs
  - crates/memcp-core/src/benchmark/runner.rs
  - crates/memcp-core/src/benchmark/mod.rs
  - crates/memcp-core/src/cli.rs
  - crates/memcp-core/src/config.rs
  - crates/memcp-core/src/load_test/metrics.rs
  - crates/memcp-core/src/load_test/report.rs
  - crates/memcp-core/src/load_test/trust.rs
  - crates/memcp-core/src/logging.rs
autonomous: true
requirements: [TWR-02]

must_haves:
  truths:
    - "A contributor cloning the repo can run cargo clippy -- -D warnings and see a clean build with zero warnings"
    - "All existing tests pass after lint fixes — no behavioral changes introduced"
    - "cargo fmt --check passes with no formatting regressions"
  artifacts:
    - path: "crates/memcp-core/src/"
      provides: "All source files pass clippy without warnings"
  key_links:
    - from: "all modified files"
      to: "cargo clippy"
      via: "zero warnings"
      pattern: "0 warnings"
---

<objective>
Fix all 117 clippy warnings across the memcp-core crate to achieve a zero-warning build.

Purpose: Pre-release code quality bar — zero clippy warnings signals a maintained, professional codebase for open-source contributors.
Output: All source files pass `cargo clippy -- -D warnings` without errors or warnings.
</objective>

<execution_context>
@/Users/ayoamadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoamadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-system-review/11-CONTEXT.md
@.planning/phases/11-system-review/11-RESEARCH.md
@.planning/phases/11-system-review/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix clippy warnings in pipeline/ modules</name>
  <files>
    crates/memcp-core/src/pipeline/auto_store/filter.rs,
    crates/memcp-core/src/pipeline/auto_store/parser.rs,
    crates/memcp-core/src/pipeline/auto_store/watcher.rs,
    crates/memcp-core/src/pipeline/auto_store/mod.rs,
    crates/memcp-core/src/pipeline/consolidation/similarity.rs,
    crates/memcp-core/src/pipeline/consolidation/mod.rs,
    crates/memcp-core/src/pipeline/content_filter/regex_filter.rs,
    crates/memcp-core/src/pipeline/content_filter/semantic_filter.rs,
    crates/memcp-core/src/pipeline/content_filter/mod.rs,
    crates/memcp-core/src/pipeline/extraction/ollama.rs,
    crates/memcp-core/src/pipeline/extraction/openai.rs,
    crates/memcp-core/src/pipeline/extraction/pipeline.rs,
    crates/memcp-core/src/pipeline/extraction/mod.rs,
    crates/memcp-core/src/pipeline/gc/worker.rs,
    crates/memcp-core/src/pipeline/gc/dedup.rs,
    crates/memcp-core/src/pipeline/gc/mod.rs,
    crates/memcp-core/src/pipeline/summarization/ollama.rs,
    crates/memcp-core/src/pipeline/summarization/openai.rs,
    crates/memcp-core/src/pipeline/summarization/mod.rs,
    crates/memcp-core/src/pipeline/curation/mod.rs,
    crates/memcp-core/src/pipeline/curation/worker.rs,
    crates/memcp-core/src/pipeline/curation/algorithmic.rs
  </files>
  <action>
    Run `cargo clippy 2>&1 | grep "crates/memcp-core/src/pipeline/"` to get warnings specific to pipeline/ modules. Fix all warnings in this directory tree.

    Common warning types to expect in pipeline/:
    - **`empty_line_after_doc_comments`**: Module-level `///` comments at file top → convert to `//!` inner doc comments. Empty lines between `///` and the item → remove the empty line.
    - **`needless_borrows_for_generic_args`**: Remove unnecessary `&` on arguments where the owned value already implements the trait.
    - **`wildcard_in_or_patterns`**: Remove redundant arms before wildcard in match expressions.
    - **`map_or` → `is_some_and`**: Replace `.map_or(false, |x| ...)` with `.is_some_and(|x| ...)`.
    - **`redundant_closure`**: Replace `.map(|x| func(x))` with `.map(func)`.
    - **`impl_can_be_derived`**: Replace manual `impl Default` with `#[derive(Default)]`.

    **IMPORTANT:** Do NOT change any logic or behavior. These are purely cosmetic/lint fixes. If a fix seems like it could change behavior, leave it with `#[allow(clippy::...)]` and a comment.

    After fixes, run `cargo build` to verify compilation. Then run `cargo test --lib -- --test-threads=1` for pipeline-related tests.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo clippy 2>&1 | grep -c "src/pipeline/" | xargs test 0 -eq</automated>
  </verify>
  <done>Zero clippy warnings in all pipeline/ source files, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix clippy warnings in intelligence/ + storage/ modules</name>
  <files>
    crates/memcp-core/src/intelligence/embedding/local.rs,
    crates/memcp-core/src/intelligence/embedding/openai.rs,
    crates/memcp-core/src/intelligence/embedding/pipeline.rs,
    crates/memcp-core/src/intelligence/query_intelligence/ollama.rs,
    crates/memcp-core/src/intelligence/query_intelligence/openai.rs,
    crates/memcp-core/src/intelligence/query_intelligence/temporal.rs,
    crates/memcp-core/src/intelligence/query_intelligence/mod.rs,
    crates/memcp-core/src/intelligence/recall/mod.rs,
    crates/memcp-core/src/intelligence/search/salience.rs,
    crates/memcp-core/src/storage/store/postgres.rs
  </files>
  <action>
    Run `cargo clippy 2>&1 | grep -E "src/(intelligence|storage)/"` to get warnings specific to these modules. Fix all warnings.

    Common warning types to expect:
    - **`empty_line_after_doc_comments`**: Convert module-level `///` to `//!`, remove empty lines between doc comments and items.
    - **`needless_borrows_for_generic_args`**: Remove unnecessary `&` borrows.
    - **`redundant_guard`**: Remove redundant guard conditions in match arms.
    - **`if_let_can_be_collapsed`**: Collapse nested `if let` or restructure.

    **IMPORTANT:** Do NOT change any logic or behavior. Purely cosmetic/lint fixes.

    After fixes, run `cargo build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo clippy 2>&1 | grep -cE "src/(intelligence|storage)/" | xargs test 0 -eq</automated>
  </verify>
  <done>Zero clippy warnings in intelligence/ and storage/ source files, compilation succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Fix clippy warnings in transport/ + import/ modules</name>
  <files>
    crates/memcp-core/src/transport/api/delete.rs,
    crates/memcp-core/src/transport/api/search.rs,
    crates/memcp-core/src/transport/daemon.rs,
    crates/memcp-core/src/transport/server.rs,
    crates/memcp-core/src/transport/health/mod.rs,
    crates/memcp-core/src/import/batch.rs,
    crates/memcp-core/src/import/chatgpt.rs,
    crates/memcp-core/src/import/claude_ai.rs,
    crates/memcp-core/src/import/export/markdown.rs,
    crates/memcp-core/src/import/export/mod.rs,
    crates/memcp-core/src/import/markdown.rs,
    crates/memcp-core/src/import/openclaw.rs
  </files>
  <action>
    Run `cargo clippy 2>&1 | grep -E "src/(transport|import)/"` to get warnings specific to these modules. Fix all warnings.

    Common warning types to expect:
    - **`empty_line_after_doc_comments`**: Convert module-level `///` to `//!`, remove empty lines.
    - **`needless_borrows_for_generic_args`**: Remove unnecessary `&` borrows.
    - **`useless_format`**: Replace `format!("literal")` with `"literal".to_string()`.
    - **`redundant_pattern_matching`**: Replace `if let Err(_) = x` with `if x.is_err()`.

    **IMPORTANT:** Do NOT change any logic or behavior. Purely cosmetic/lint fixes.

    After fixes, run `cargo build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo clippy 2>&1 | grep -cE "src/(transport|import)/" | xargs test 0 -eq</automated>
  </verify>
  <done>Zero clippy warnings in transport/ and import/ source files, compilation succeeds</done>
</task>

<task type="auto">
  <name>Task 4: Fix clippy warnings in benchmark/ + cli.rs + config.rs + remaining files</name>
  <files>
    crates/memcp-core/src/benchmark/dataset.rs,
    crates/memcp-core/src/benchmark/evaluate.rs,
    crates/memcp-core/src/benchmark/ingest.rs,
    crates/memcp-core/src/benchmark/prompts.rs,
    crates/memcp-core/src/benchmark/report.rs,
    crates/memcp-core/src/benchmark/runner.rs,
    crates/memcp-core/src/benchmark/mod.rs,
    crates/memcp-core/src/cli.rs,
    crates/memcp-core/src/config.rs,
    crates/memcp-core/src/load_test/metrics.rs,
    crates/memcp-core/src/load_test/report.rs,
    crates/memcp-core/src/load_test/trust.rs,
    crates/memcp-core/src/logging.rs
  </files>
  <action>
    Fix all remaining clippy warnings not covered by Tasks 1-3.

    Run `cargo clippy 2>&1` and address everything still flagged.

    Special handling for this batch:
    - **`too_many_arguments` (~7 warnings across 3 functions in cli.rs):** For functions with 9-13 args: add `#[allow(clippy::too_many_arguments)]` with a comment explaining why a struct refactor is deferred (breaking change scope). These are internal functions, not public API.
    - **`vec_init_then_push` / `useless_vec`**: Use array literal or `vec![...]` initialization instead of push loop.
    - **`write_mut_vec`**: Change `&mut Vec<_>` to `&mut [_]` in function signatures where only slice operations are used.
    - **`map_or` → `is_some_and`**: Replace `.map_or(false, |x| ...)` patterns in load_test/trust.rs and others.

    **IMPORTANT:** Do NOT change any logic or behavior.

    After all fixes: run `cargo clippy -- -D warnings` — must succeed with zero warnings. Then run `cargo test --lib -- --test-threads=1` to verify no regressions across the full test suite.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo clippy -- -D warnings 2>&1 | tail -1 | grep -q "Finished"</automated>
  </verify>
  <done>cargo clippy -- -D warnings passes cleanly (zero warnings, zero errors), all tests pass, no behavioral changes</done>
</task>

</tasks>

<verification>
- `cargo clippy -- -D warnings` exits with code 0
- `cargo test --lib -- --test-threads=1` passes (same count as before, minus the locomo test now ignored)
- `cargo fmt --check` passes (no formatting regressions)
</verification>

<success_criteria>
- A contributor cloning the repo can run cargo clippy -- -D warnings and see a clean build
- All existing tests continue to pass
- No behavioral changes introduced
</success_criteria>

<output>
After completion, create `.planning/phases/11-system-review/11-02-SUMMARY.md`
</output>
