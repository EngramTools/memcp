---
phase: 08.8-plugin-support-primitives
plan: 02
subsystem: cli
tags: [rust, cli, mcp, annotate, tags, salience, fsrs]

# Dependency graph
requires:
  - phase: 08.8-01
    provides: event_time/workspace schema and config foundations
provides:
  - annotate_logic() shared helper in cli.rs for tag append/replace and salience absolute/multiplier
  - memcp annotate CLI subcommand with --id, --tags, --replace-tags, --salience args
  - annotate_memory MCP tool with same semantics and inline examples in description
  - Diff response format: tags_added, tags_removed, salience_before, salience_after
affects: [08.8-03, 08.8-04, 08.8-05, plugin-enrichment, mcp-tools]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "annotate_logic() shared helper called by both CLI (cmd_annotate) and MCP (annotate_memory) — single source of truth for annotate semantics"
    - "Salience read-before-write pattern: get_salience_data() before upsert_salience() to preserve FSRS difficulty and reinforcement_count"
    - "tag mutation via HashSet: deduplicate and sort when appending; direct replace when replace_tags given"
    - "Salience input parsing: ends_with('x') = multiplier mode, else absolute mode"

key-files:
  created: []
  modified:
    - crates/memcp/src/main.rs
    - crates/memcp-core/src/cli.rs
    - crates/memcp-core/src/transport/server.rs
    - crates/memcp-core/tests/integration_test.rs

key-decisions:
  - "annotate_logic() extracted as shared pub fn in cli.rs — CLI and MCP both call it, no logic duplication"
  - "Memory.tags is Option<serde_json::Value> (JSONB) — parsed to Vec<String> via as_array() + as_str() iterator chain"
  - "UpdateMemory struct has only 4 fields (content, type_hint, source, tags) — no actor/actor_type/audience; plan interfaces section was aspirational"
  - "annotate_memory added to tool_router_with_meta() allowed_callers alongside search_memory/store_memory/recall_memory — non-destructive enrichment mutation"
  - "test_tool_discovery updated from 11 to 12 tool count (Rule 1 auto-fix)"

patterns-established:
  - "Shared annotate_logic pattern: extract shared mutation logic into pub async fn in cli.rs, call from both cmd_ handler and MCP tool method"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-02-28
---

# Phase 08.8 Plan 02: Annotate Command Summary

**memcp annotate CLI + annotate_memory MCP tool with tag append/replace and salience absolute/multiplier, returning diff JSON**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-28T08:34:04Z
- **Completed:** 2026-02-28T08:38:34Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- CLI `memcp annotate --id X --tags t1,t2` appends tags and outputs diff JSON
- CLI `memcp annotate --id X --replace-tags t1,t2` replaces all tags
- CLI `memcp annotate --id X --salience 0.9` sets absolute stability
- CLI `memcp annotate --id X --salience 1.5x` multiplies current stability
- MCP `annotate_memory` tool with same semantics; included in allowed_callers for sandbox access
- Salience mutation preserves FSRS difficulty and reinforcement_count via read-before-write
- All 68 unit tests pass; 1 integration test updated for new tool count

## Task Commits

1. **Task 1: CLI annotate command + handler** - `3487c0d` (feat)
2. **Task 2: MCP annotate_memory tool** - `164a537` (feat)

## Files Created/Modified

- `crates/memcp/src/main.rs` - Added Annotate subcommand variant and match arm
- `crates/memcp-core/src/cli.rs` - Added annotate_logic() shared helper, cmd_annotate() handler, UpdateMemory + HashSet imports
- `crates/memcp-core/src/transport/server.rs` - Added AnnotateMemoryParams struct, annotate_memory tool method, updated tool_router_with_meta() callers list
- `crates/memcp-core/tests/integration_test.rs` - Updated tool count from 11 to 12, added annotate_memory assertion

## Decisions Made

- `annotate_logic()` extracted as shared `pub async fn` in `cli.rs` — CLI and MCP call it directly, no logic duplication per plan recommendation
- `Memory.tags` is `Option<serde_json::Value>` (JSONB) not `Vec<String>` — parsed via `.as_array().filter_map(as_str)` chain
- `UpdateMemory` struct only has 4 fields (`content`, `type_hint`, `source`, `tags`) — plan interfaces section listed more fields (`actor`, `actor_type`, `audience`) that don't exist; used actual struct shape
- `annotate_memory` added to `tool_router_with_meta()` allowed_callers — non-destructive enrichment belongs alongside `store_memory`/`recall_memory`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated test_tool_discovery tool count from 11 to 12**
- **Found during:** Task 2 (MCP annotate_memory tool)
- **Issue:** Integration test expected exactly 11 tools; adding annotate_memory made it 12, causing test failure
- **Fix:** Updated assertion to 12 and added `assert!(tool_names.contains(&"annotate_memory".to_string()))`
- **Files modified:** `crates/memcp-core/tests/integration_test.rs`
- **Verification:** All 68 tests pass after fix
- **Committed in:** `164a537` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug: hardcoded tool count in test)
**Impact on plan:** Auto-fix necessary for test suite correctness. No scope creep.

## Issues Encountered

- `Memory.tags` is `Option<serde_json::Value>` (JSONB), not `Vec<String>` as shown in plan interfaces. Fixed by extracting via serde_json array iteration.
- `UpdateMemory` struct lacked `actor`/`actor_type`/`audience` fields shown in plan interfaces. Used actual struct with 4 fields only.

## Next Phase Readiness

- Plan 03 (event_time parsing) can proceed — annotate infrastructure is in place
- Plugin enrichment pattern established: plugins can call `annotate_memory` to add semantic tags (decision, preference, correction) and boost salience on key moments

---
*Phase: 08.8-plugin-support-primitives*
*Completed: 2026-02-28*
