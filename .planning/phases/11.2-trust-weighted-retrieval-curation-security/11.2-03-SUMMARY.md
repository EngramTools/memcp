---
phase: 11.2-trust-weighted-retrieval-curation-security
plan: 03
subsystem: pipeline
tags: [curation, injection-detection, priority-queue, llm-prompt, trust]

requires:
  - phase: 11.2-02
    provides: "CurationAction::Suspicious variant, ClusterMember.trust_level field, algorithmic injection detection"
provides:
  - "LLM instruction-detection via extended curation prompt (zero extra API cost)"
  - "parse_review_response handles suspicious action from LLM output"
  - "Priority queue ordering: P1 (low trust + new) -> P2 (medium trust + new) -> Normal"
  - "Priority-aware LLM routing: P1/P2 get LLM review, Normal gets algorithmic only"
  - "format_cluster includes trust_level for LLM context"
affects: []

tech-stack:
  added: []
  patterns: ["priority-aware provider selection per cluster", "trust-level in LLM prompt context"]

key-files:
  created: []
  modified:
    - "crates/memcp-core/src/pipeline/curation/ollama.rs"
    - "crates/memcp-core/src/pipeline/curation/worker.rs"

key-decisions:
  - "P1/P2 clusters get LLM review, Normal clusters get algorithmic only — concentrates LLM budget on risky memories"
  - "Priority sorting before clustering ensures low-trust memories become cluster seeds first"

patterns-established:
  - "Per-cluster provider selection based on member priority"

requirements-completed: [TWR-07, TWR-08]

duration: 5min
completed: 2026-03-09
---

# Phase 11.2 Plan 03: LLM Instruction-Detection + Priority Curation Summary

**Extended curation LLM prompt with injection-detection dimension (zero extra cost) and priority queue ordering that routes low-trust new memories to LLM review first**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-09T05:24:03Z
- **Completed:** 2026-03-09T05:29:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- REVIEW_SYSTEM_PROMPT extended with SUSPICIOUS action for semantic injection detection at zero extra API cost
- parse_review_response handles all 5 action types (merge, flag-stale, strengthen, suspicious, skip)
- format_cluster now includes trust_level per member for LLM context
- Priority queue: candidates sorted P1 -> P2 -> Normal before clustering
- Per-cluster LLM routing: P1/P2 clusters get LLM review, Normal gets algorithmic only
- 8 new tests (3 ollama + 5 worker), all 22 curation tests passing

## Task Commits

Each task was committed atomically:

1. **Task 1: LLM instruction-detection prompt + parse_review_response** - `a24cc52` (test RED), `057fd5b` (feat GREEN)
2. **Task 2: Priority queue ordering in curation worker** - `8486164` (test + priority_score), `eeedb15` (feat: integration)

_TDD tasks have multiple commits (test -> feat)_

## Files Created/Modified
- `crates/memcp-core/src/pipeline/curation/ollama.rs` - Extended REVIEW_SYSTEM_PROMPT with suspicious action, updated parse_review_response, format_cluster with trust_level
- `crates/memcp-core/src/pipeline/curation/worker.rs` - priority_score function, cluster_has_high_priority, candidate sorting, per-cluster provider selection

## Decisions Made
- P1/P2 clusters get LLM review, Normal clusters get algorithmic only — concentrates LLM budget on risky memories without wasting calls on trusted content
- Priority sorting happens before clustering so low-trust memories become cluster seeds first, pulling in related memories for joint review

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 11.2 complete (all 3 plans done)
- Trust-weighted retrieval, curation security, and LLM instruction-detection all operational
- Ready for next phase

## Self-Check: PASSED

All files exist, all 4 commits verified, key patterns confirmed in source files.

---
*Phase: 11.2-trust-weighted-retrieval-curation-security*
*Completed: 2026-03-09*
