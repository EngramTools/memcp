---
phase: 08.7-multi-model-embeddings
plan: 03
subsystem: pipeline
tags: [rust, embeddings, promotion, daemon, background-workers, tiered-embeddings]

# Dependency graph
requires:
  - phase: 08.7-01
    provides: EmbeddingRouter, PromotionConfig, get_promotion_candidates, insert_embedding, deactivate_tier_embedding
provides:
  - PromotionResult struct with promoted_count, candidates_evaluated, failed_count, skipped_reason
  - run_promotion_sweep function — batch-capped fail-open promotion from fast to quality tier
  - Daemon spawn block for promotion sweep worker (interval-based, multi-model only)
affects: [08.7-04, multi-tier-search]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Promotion sweep mirrors curation sweep: tokio::spawn interval loop, fail-open per-item, batch_cap limit"
    - "Fail-open per-item promotion: embedding failure logs warning + increments failed_count, never panics"
    - "Single source tier deactivated before target tier inserted — prevents duplicate current embeddings"

key-files:
  created:
    - crates/memcp-core/src/pipeline/promotion/mod.rs
    - crates/memcp-core/src/pipeline/promotion/worker.rs
  modified:
    - crates/memcp-core/src/pipeline/mod.rs
    - crates/memcp-core/src/lib.rs
    - crates/memcp-core/src/transport/daemon.rs

key-decisions:
  - "Promotion sweep pre-implemented in 08.7-01 commit (0f6d57b) as forward-work — all three plan tasks were already committed"
  - "Sweep skipped entirely when no quality tier configured (router.is_multi_model() = false)"
  - "Candidates fetched via get_promotion_candidates joining memory_salience for stability + reinforcement thresholds"
  - "Old fast-tier embedding deactivated (is_current=false) before new quality-tier embedding inserted"

patterns-established:
  - "Promotion sweep: deactivate old tier embedding → insert new tier embedding (not alongside)"
  - "Daemon worker spawn gated on router.is_multi_model() && quality tier has PromotionConfig"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-02-28
---

# Phase 08.7 Plan 03: Promotion Sweep Worker Summary

**Periodic promotion sweep that re-embeds important memories (high stability + reinforcements) from fast local model to quality API model, wired into daemon with configurable interval and batch cap**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-28T08:19:09Z
- **Completed:** 2026-02-28T08:24:00Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments
- `PromotionResult` struct with promoted_count, candidates_evaluated, failed_count, skipped_reason
- `run_promotion_sweep` — fetches candidates, embeds with quality provider, deactivates old + inserts new embedding
- Daemon spawn block mirrors curation worker pattern, gated on multi-model config with interval-based sweep
- 68 unit tests passing, cargo build clean

## Task Commits

All tasks were pre-implemented and committed atomically in plan 08.7-01:

1. **Task 1: Create promotion module structure** - `0f6d57b` (feat: extend insert_embedding with tier param, add HNSW and store methods)
2. **Task 2: Implement run_promotion_sweep worker** - `0f6d57b` (feat: same commit — promotion/ pre-work for Plan 03)
3. **Task 3: Wire promotion sweep into daemon** - `0f6d57b` (feat: daemon.rs promotion sweep spawn block)

## Files Created/Modified
- `crates/memcp-core/src/pipeline/promotion/mod.rs` - PromotionResult struct + module exports
- `crates/memcp-core/src/pipeline/promotion/worker.rs` - run_promotion_sweep with fail-open per-item logic
- `crates/memcp-core/src/pipeline/mod.rs` - added `pub mod promotion`
- `crates/memcp-core/src/lib.rs` - added `pub use pipeline::promotion`
- `crates/memcp-core/src/transport/daemon.rs` - spawn block section 8.7 for promotion sweep

## Decisions Made
- Pre-implementation in 08.7-01: all three plan tasks were completed ahead of schedule as forward-work during the embedding foundation plan; no new commits needed in this plan
- Sweep skips with `skipped_reason` when no candidates found (not an error, just a quiet no-op)
- Daemon block at section 8.7 fires only when `router.is_multi_model()` AND quality tier has a `PromotionConfig`

## Deviations from Plan

None — plan executed exactly as written. All tasks were already committed in 08.7-01; verification confirmed build and tests pass.

## Issues Encountered
None — cargo build clean, 68 tests passing (DB tests require DATABASE_URL=postgres://memcp:memcp@localhost:5433/memcp).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Promotion sweep fully operational — ready for Plan 04 (multi-tier search)
- run_promotion_sweep is wired into daemon but won't fire unless a quality tier with PromotionConfig is configured

---
*Phase: 08.7-multi-model-embeddings*
*Completed: 2026-02-28*
