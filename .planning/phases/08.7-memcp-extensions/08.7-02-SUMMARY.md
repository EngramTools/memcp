---
phase: 08.7-multi-model-embeddings
plan: 02
subsystem: embedding
tags: [rust, embedding, multi-tier, router, auto-store, pipeline]

# Dependency graph
requires:
  - phase: 08.7-01
    provides: EmbeddingRouter, EmbeddingJob.tier, EmbeddingPipeline::new(router), build_embedding_router, migration 017

provides:
  - Auto-store sidecar wired to EmbeddingRouter for tier-aware job submission
  - Chunks inherit parent memory's tier (consistent per-document embedding)
  - Daemon passes router to auto-store; serve mode uses None (backward compat)

affects: [08.7-03, promotion-sweep, auto-store]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Option<Arc<EmbeddingRouter>> threaded through spawn() so serve mode (None) and daemon mode (Some) share one code path"
    - "Chunks inherit parent tier — one router.route() call per logical document, not per chunk"

key-files:
  created: []
  modified:
    - crates/memcp-core/src/pipeline/auto_store/mod.rs
    - crates/memcp-core/src/transport/daemon.rs
    - crates/memcp/src/main.rs

key-decisions:
  - "Tasks 1-3 were pre-implemented during Plan 01 execution: EmbeddingJob.tier, EmbeddingCompletion.tier, EmbeddingPipeline::new(router)/new_single(), build_embedding_router, create_tier_provider, per-tier HNSW index creation, IPC listener using default_provider()"
  - "Auto-store tier routing uses Option<Arc<EmbeddingRouter>> — None in serve mode (single-tier backward compat), Some(router) in daemon mode"
  - "New memories route with stability=None so router returns 'fast' by default; promotion sweep (Plan 03) handles quality-tier upgrade later"
  - "Chunks inherit parent tier — router.route() called once per parent, chunk uses same tier string"

patterns-established:
  - "Optional router pattern: Option<Arc<EmbeddingRouter>> with None fallback to 'fast' — consistent across auto-store spawn + serve mode"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-02-28
---

# Phase 08.7 Plan 02: Multi-Tier Pipeline Wiring Summary

**EmbeddingRouter wired into auto-store sidecar for tier-aware embedding job submission, with chunks inheriting parent tier and daemon passing the live router at startup**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-02-28T08:07:00Z
- **Completed:** 2026-02-28T08:22:06Z
- **Tasks:** 4 (Tasks 1-3 pre-done in Plan 01; Task 4 implemented here)
- **Files modified:** 3

## Accomplishments
- Discovered Tasks 1-3 were fully pre-implemented during Plan 01 execution (EmbeddingJob.tier, EmbeddingPipeline::new(router), build_embedding_router, create_tier_provider, per-tier HNSW indexes, IPC listener)
- Added `embedding_router: Option<Arc<EmbeddingRouter>>` to AutoStoreWorker::spawn() and run_worker()
- Daemon auto-store path uses live router for tier selection; serve mode and main.rs pass None (backward compat)
- Chunks inherit parent memory's tier — single router.route() call per logical document
- 68 tests passing, clean build

## Task Commits

1. **Task 4: Auto-store tier routing via EmbeddingRouter** - `5b59106` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `crates/memcp-core/src/pipeline/auto_store/mod.rs` - Added router import, embedding_router param to spawn()/run_worker(), tier selection logic using router.route(), chunk tier inheritance
- `crates/memcp-core/src/transport/daemon.rs` - Pass Some(router.clone()) to auto-store spawn
- `crates/memcp/src/main.rs` - Pass None for router in serve mode (backward compat)

## Decisions Made
- Tasks 1-3 were pre-built in Plan 01 — no redundant commits created; plan is complete as-is
- serve mode (main.rs) passes None — single-tier, no need for router overhead
- New auto-stored memories use stability=None which always routes to "fast" default tier; this is correct behavior since routing to quality requires established stability signals

## Deviations from Plan

None - Tasks 1-3 were discovered pre-complete from Plan 01 pre-work. Task 4 executed exactly as specified.

## Issues Encountered
- main.rs also had a `AutoStoreWorker::spawn()` call site (serve mode path) that needed the new `None` argument — caught by the compiler, fixed as Rule 3 (blocking).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Multi-tier embedding pipeline fully wired: router creation, per-tier HNSW indexes, pipeline routing, auto-store routing
- Ready for Plan 03: Promotion sweep worker (re-embedding fast-tier memories to quality tier based on reinforcement/stability signals)

---
*Phase: 08.7-multi-model-embeddings*
*Completed: 2026-02-28*
