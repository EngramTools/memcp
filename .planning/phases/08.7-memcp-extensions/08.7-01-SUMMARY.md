---
phase: 08.7-multi-model-embeddings
plan: 01
subsystem: database
tags: [rust, postgres, pgvector, embeddings, multi-model, hnsw]

# Dependency graph
requires:
  - phase: 07-02
    provides: EmbeddingConfig with local_model/openai_model/dimension fields and model_dimension() helper
  - phase: 08-03
    provides: Cargo workspace layout (memcp-core crate)
provides:
  - EmbeddingTierConfig, RoutingConfig, PromotionConfig config structs
  - EmbeddingConfig.tiers HashMap for named multi-model tiers
  - Migration 017 with tier column on memory_embeddings
  - EmbeddingRouter for multi-tier routing at store time
  - insert_embedding with tier param
  - ensure_hnsw_index_for_tier for partial per-tier indexes
  - count_tier_embeddings and get_promotion_candidates store methods
  - build_embedding_router and create_tier_provider in daemon.rs
affects:
  - 08.7-02
  - 08.7-03
  - 08.7-04

# Tech tracking
tech-stack:
  added: []
  patterns:
    - AND-logic routing rules — all specified conditions (min_stability, type_hints, min_content_length) must be met for a tier to be selected
    - Single-tier passthrough — empty tiers HashMap = legacy single-model mode, zero config change required
    - Partial HNSW indexes per tier using WHERE tier='name' with correct dimension cast

key-files:
  created:
    - crates/memcp-core/migrations/017_embedding_tiers.sql
    - crates/memcp-core/src/intelligence/embedding/router.rs
    - crates/memcp-core/src/pipeline/promotion/mod.rs
    - crates/memcp-core/src/pipeline/promotion/worker.rs
  modified:
    - crates/memcp-core/src/config.rs
    - crates/memcp-core/src/intelligence/embedding/mod.rs
    - crates/memcp-core/src/intelligence/embedding/pipeline.rs
    - crates/memcp-core/src/storage/store/postgres.rs
    - crates/memcp-core/src/transport/daemon.rs

key-decisions:
  - "EmbeddingTierConfig: provider, model, openai_api_key, dimension, routing (RoutingConfig), promotion (PromotionConfig) — all optional except provider"
  - "RoutingConfig uses AND logic: all specified conditions must be met; omitted conditions are not checked"
  - "PromotionConfig defaults: min_reinforcements=3, min_stability=0.8, sweep_interval=60min, batch_cap=15"
  - "EmbeddingRouter implements EmbeddingProvider (delegates to default tier) for drop-in backward compat with pipeline"
  - "EmbeddingPipeline.new() takes Arc<EmbeddingRouter>; new_single() wraps single provider for serve mode and tests"
  - "build_embedding_router in daemon.rs: legacy mode wraps create_embedding_provider in single-tier router; multi-tier creates per-tier providers via create_tier_provider"
  - "ensure_hnsw_index_for_tier creates partial indexes WHERE tier='name' with dimension-aware cast"
  - "Promotion sweep module included in Plan 01 commit because daemon.rs references it (required for compilation)"

patterns-established:
  - "Tier routing: route() checks non-default tiers in iteration order; first matching tier wins; default tier is fallback"
  - "Lazy quality tier skip: embed_query_all_tiers checks count_tier_embeddings before calling non-default provider"

requirements-completed: []

# Metrics
duration: 25min
completed: 2026-02-28
---

# Phase 08.7 Plan 01: Multi-Model Embedding Foundation Summary

**EmbeddingTierConfig structs, migration 017 (tier column), EmbeddingRouter with AND-logic routing, and per-tier HNSW partial indexes — zero-config backward compat via empty tiers HashMap**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-02-28T07:50:00Z
- **Completed:** 2026-02-28T08:15:32Z
- **Tasks:** 4
- **Files modified:** 14

## Accomplishments

- Config foundation: RoutingConfig (AND logic), PromotionConfig (defaults 3/0.8/60m/15), EmbeddingTierConfig, tiers HashMap on EmbeddingConfig with is_multi_model() and default_tier_name()
- Migration 017: tier TEXT NOT NULL DEFAULT 'fast' on memory_embeddings with two partial indexes for sweep queries
- EmbeddingRouter: routes by type_hint+stability+content_length, implements EmbeddingProvider for drop-in backward compat, 8 unit tests passing
- Store layer extended: insert_embedding gains tier param, ensure_hnsw_index_for_tier, count_tier_embeddings, get_promotion_candidates, deactivate_tier_embedding
- All EmbeddingJob callers updated with tier: "fast" default; pipeline uses Arc<EmbeddingRouter>; serve/backfill paths use new_single()

## Task Commits

1. **Task 1: Config structs** - `12bcaf1` (feat)
2. **Task 2: Migration 017** - `539d993` (feat)
3. **Task 3: EmbeddingRouter** - `66e4d86` (feat)
4. **Task 4: insert_embedding tier + store methods + callers** - `0f6d57b` (feat)

## Files Created/Modified

- `crates/memcp-core/migrations/017_embedding_tiers.sql` - tier column + 2 partial indexes
- `crates/memcp-core/src/config.rs` - RoutingConfig, PromotionConfig, EmbeddingTierConfig, tiers HashMap
- `crates/memcp-core/src/intelligence/embedding/router.rs` - EmbeddingRouter implementation
- `crates/memcp-core/src/intelligence/embedding/mod.rs` - pub mod router re-export
- `crates/memcp-core/src/intelligence/embedding/pipeline.rs` - EmbeddingPipeline::new(Arc<EmbeddingRouter>), new_single()
- `crates/memcp-core/src/storage/store/postgres.rs` - tier param on insert_embedding, 4 new methods
- `crates/memcp-core/src/transport/daemon.rs` - build_embedding_router, create_tier_provider, per-tier HNSW, promotion sweep wiring
- `crates/memcp-core/src/pipeline/promotion/mod.rs` - PromotionResult struct (pre-work for Plan 03)
- `crates/memcp-core/src/pipeline/promotion/worker.rs` - run_promotion_sweep (pre-work for Plan 03)

## Decisions Made

- EmbeddingRouter implements EmbeddingProvider (delegates to default tier) — allows drop-in use in contexts expecting Arc<dyn EmbeddingProvider>
- build_embedding_router in daemon.rs handles legacy → single-tier router; multi-model → per-tier providers
- Promotion module included in Plan 01 because daemon.rs section 8.7 references it, required for compilation

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Included promotion module to enable daemon.rs compilation**
- **Found during:** Task 4 (daemon.rs wiring)
- **Issue:** daemon.rs references `crate::promotion::worker::run_promotion_sweep` in the promotion sweep section. Without the promotion module, the crate would not compile.
- **Fix:** Included `pipeline/promotion/mod.rs` and `pipeline/promotion/worker.rs` in Task 4 commit. These implement `run_promotion_sweep` which daemon.rs calls. This is pre-work for Plan 03 but was required for Task 4's daemon wiring to compile.
- **Files modified:** crates/memcp-core/src/pipeline/promotion/mod.rs, worker.rs, pipeline/mod.rs, lib.rs
- **Verification:** `cargo build` passes; `cargo test` passes (68 + router tests)
- **Committed in:** 0f6d57b (Task 4 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Promotion module is Plan 03 scope implemented in Plan 01 commit. No functional change to Plan 01's scope; Plan 03 can build on the existing implementation.

## Issues Encountered

None — all code was already implemented in the working tree. Plan 01 execution consisted of verifying the implementation, building, running tests (all passing), and committing in per-task atomic commits.

## Next Phase Readiness

- Plan 02 can build the multi-tier search (dual-query, hybrid_search_multi_tier, RRF merge)
- Plan 03 promotion sweep worker is already implemented in `pipeline/promotion/`; Plan 03 can wire daemon integration
- 68 unit tests + 8 router tests all passing
- Migration 017 is in place for DB schema changes

---
*Phase: 08.7-multi-model-embeddings*
*Completed: 2026-02-28*
