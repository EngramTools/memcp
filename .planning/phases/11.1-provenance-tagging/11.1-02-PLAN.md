---
phase: 11.1-provenance-tagging
plan: 02
type: execute
wave: 2
depends_on:
  - 11.1-01
files_modified:
  - crates/memcp-core/src/transport/server.rs
  - crates/memcp-core/src/transport/api/types.rs
  - crates/memcp-core/src/transport/api/mod.rs
  - crates/memcp-core/src/pipeline/auto_store/mod.rs
  - crates/memcp-core/src/cli.rs
autonomous: true
requirements:
  - PROV-07
  - PROV-08
  - PROV-09
  - PROV-10

must_haves:
  truths:
    - "MCP store_memory tool accepts trust_level, session_id, agent_role params and passes them through to store"
    - "HTTP API store endpoint accepts trust_level, session_id, agent_role in request body"
    - "Auto-store worker populates session_id column from parsed log entry AND sets trust_level=0.3"
    - "CLI store command accepts --trust-level, --session-id, --agent-role flags"
    - "All write paths set source correctly (never 'default' for known paths)"
  artifacts:
    - path: "crates/memcp-core/src/transport/server.rs"
      provides: "MCP tool param structs with provenance fields"
      contains: "trust_level"
    - path: "crates/memcp-core/src/transport/api/types.rs"
      provides: "HTTP API request types with provenance fields"
      contains: "session_id"
    - path: "crates/memcp-core/src/pipeline/auto_store/mod.rs"
      provides: "Auto-store CreateMemory with session_id column + trust_level=0.3"
      contains: "session_id"
    - path: "crates/memcp-core/src/cli.rs"
      provides: "CLI flags for provenance fields"
      contains: "agent_role"
  key_links:
    - from: "crates/memcp-core/src/transport/server.rs"
      to: "crates/memcp-core/src/storage/store/mod.rs"
      via: "StoreMemoryParams → CreateMemory conversion"
      pattern: "trust_level.*session_id.*agent_role"
    - from: "crates/memcp-core/src/transport/api/types.rs"
      to: "crates/memcp-core/src/storage/store/mod.rs"
      via: "StoreRequest → CreateMemory conversion"
      pattern: "trust_level.*session_id.*agent_role"
    - from: "crates/memcp-core/src/pipeline/auto_store/mod.rs"
      to: "crates/memcp-core/src/storage/store/mod.rs"
      via: "CreateMemory construction with session_id from entry.session_id"
      pattern: "session_id.*entry\\.session_id"
---

<objective>
Propagate provenance fields through all transport layers (MCP tools, HTTP API, CLI) and update the auto-store worker to use the new session_id column and trust_level. Ensures every write path into memcp can carry provenance metadata end-to-end.

Purpose: Complete the integration so agents/users/tools can pass trust_level, session_id, and agent_role through any access path.
Output: Updated MCP tool schemas, HTTP API types, CLI flags, auto-store session_id promotion, source audit verification.
</objective>

<execution_context>
@/Users/ayoamadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoamadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11.1-provenance-tagging/11.1-CONTEXT.md
@.planning/phases/11.1-provenance-tagging/11.1-RESEARCH.md
@.planning/phases/11.1-provenance-tagging/11.1-01-SUMMARY.md

@crates/memcp-core/src/transport/server.rs
@crates/memcp-core/src/transport/api/types.rs
@crates/memcp-core/src/transport/api/mod.rs
@crates/memcp-core/src/pipeline/auto_store/mod.rs
@crates/memcp-core/src/cli.rs

<interfaces>
<!-- From Plan 01 outputs (store/mod.rs) -->

CreateMemory now has (added by Plan 01):
- trust_level: Option<f32>  // None = system infers from source/actor_type
- session_id: Option<String>
- agent_role: Option<String>

UpdateMemory now has:
- trust_level: Option<f32>

ListFilter now has:
- session_id: Option<String>
- agent_role: Option<String>

infer_trust_level(source, actor_type) -> f32 is available in store::mod.

<!-- Existing transport structs to update -->

StoreMemoryParams (server.rs:236): MCP tool input. Has content, type_hint, source, tags, actor, actor_type, audience, idempotency_key, event_time, event_time_precision, project. Add trust_level, session_id, agent_role.

StoreRequest (api/types.rs:64): HTTP API input. Has content, type_hint, source, tags, actor, actor_type, audience, idempotency_key, event_time, event_time_precision, project. Add trust_level, session_id, agent_role.

UpdateRequest (api/types.rs:114): Has id, content, type_hint, source, tags. Add trust_level.

SearchRequest (api/types.rs:35): Do NOT add session_id (session filtering is list/recall only, not vector search).

<!-- Auto-store current behavior -->
auto_store/mod.rs line 263-264: Currently adds session_id as tag `session:{sid}`. Must ALSO populate CreateMemory.session_id field. Keep the tag for backward compat.
auto_store/mod.rs line 289-305: CreateMemory construction. Currently sets actor_type: "auto-store". Plan 01 added trust_level/session_id/agent_role fields — this task sets trust_level: Some(0.3) and session_id: entry.session_id.clone().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP + HTTP API transport layer provenance fields</name>
  <files>
    crates/memcp-core/src/transport/server.rs
    crates/memcp-core/src/transport/api/types.rs
    crates/memcp-core/src/transport/api/mod.rs
  </files>
  <action>
1. In `transport/server.rs`, update `StoreMemoryParams`:
   - Add `pub trust_level: Option<f32>` with schemars description: "Trust level 0.0-1.0. Omit to let memcp infer from source/actor_type."
   - Add `pub session_id: Option<String>` with description: "Session identifier for grouping memories by conversation."
   - Add `pub agent_role: Option<String>` with description: "Agent's role when creating this memory (e.g., coder, reviewer, planner). Free-text, nullable."

2. Update the `StoreMemoryParams → CreateMemory` conversion in the store_memory handler (search for where StoreMemoryParams fields are mapped to CreateMemory). Add:
   - `trust_level: params.trust_level`
   - `session_id: params.session_id`
   - `agent_role: params.agent_role`

3. If there's an `UpdateMemoryParams` in server.rs, add `trust_level: Option<f32>` and wire to UpdateMemory.

4. Update `ListMemoriesParams` (if exists in server.rs) to add session_id and agent_role filter params, wire to ListFilter.

5. In `transport/api/types.rs`, update `StoreRequest`:
   - Add `#[serde(default)] pub trust_level: Option<f32>`
   - Add `#[serde(default)] pub session_id: Option<String>`
   - Add `#[serde(default)] pub agent_role: Option<String>`

6. Update `UpdateRequest`:
   - Add `pub trust_level: Option<f32>`

7. In `transport/api/mod.rs`, update the handler that converts `StoreRequest → CreateMemory`:
   - Add trust_level, session_id, agent_role field mapping

8. Update the handler that converts `UpdateRequest → UpdateMemory`:
   - Add trust_level field mapping

9. If there's a list/search endpoint handler in api/mod.rs, update to pass session_id filter from query params to ListFilter.

10. Ensure MCP tool schema descriptions are clear enough that models use params correctly without system prompt hints (per CLAUDE.md design rule).
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo check -p memcp-core 2>&1 | tail -20</automated>
  </verify>
  <done>MCP store_memory tool accepts trust_level, session_id, agent_role. HTTP API store/update endpoints accept provenance fields. All transport-to-store conversions wire new fields correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-store session_id promotion + CLI flags + source audit</name>
  <files>
    crates/memcp-core/src/pipeline/auto_store/mod.rs
    crates/memcp-core/src/cli.rs
  </files>
  <action>
1. In `auto_store/mod.rs`, update BOTH CreateMemory construction sites (line ~289 and line ~402 for chunks):
   - Set `trust_level: Some(0.3)` (auto-store = low trust, per CONTEXT.md)
   - Set `session_id: entry.session_id.clone()` (promote from tag to column)
   - Set `agent_role: None` (auto-store doesn't know agent role)
   - KEEP the existing `tags.push(format!("session:{}", sid))` for backward compatibility

2. In `cli.rs`, update the store command:
   - Add `--trust-level <f32>` optional flag (clap arg)
   - Add `--session-id <string>` optional flag
   - Add `--agent-role <string>` optional flag
   - Wire these to CreateMemory fields in the store command handler
   - Note: CLI store should get trust_level=None by default (so infer_trust_level kicks in with source="cli" → 0.8)

3. Update CLI JSON output formatting for memory display to include trust_level, session_id, agent_role when present.

4. Source audit (PROV-10): Grep through all write paths and verify source is correctly set:
   - CLI store: source="cli" (already correct via default or explicit)
   - HTTP API: source comes from StoreRequest (default_source="default" — this is OK, callers override)
   - MCP: source comes from StoreMemoryParams (same pattern)
   - Auto-store: source from parser (e.g., "claude-code", "generic")
   - Import: source="import" or from import data
   - If any path sets source="default" when it shouldn't, fix it. The auto-store paths already set proper sources. The concern is direct store calls that rely on serde default.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && DATABASE_URL=postgres://memcp:memcp@localhost:5433/memcp cargo test -p memcp-core -- --test-threads=1 -q 2>&1 | tail -20</automated>
  </verify>
  <done>Auto-store populates session_id column AND keeps session tag. Auto-store sets trust_level=0.3. CLI accepts --trust-level, --session-id, --agent-role flags. All write paths set source correctly. Full test suite passes.</done>
</task>

</tasks>

<verification>
```bash
# Full test suite — all existing + new provenance tests must pass
DATABASE_URL=postgres://memcp:memcp@localhost:5433/memcp cargo test -- --test-threads=1

# Verify MCP schema includes new params (compile check is sufficient — schemars auto-generates)
cargo check -p memcp-core
```
</verification>

<success_criteria>
- MCP store_memory tool schema includes trust_level, session_id, agent_role parameters with clear descriptions
- HTTP API StoreRequest and UpdateRequest include provenance fields
- Auto-store CreateMemory sets trust_level=0.3 and session_id from entry
- Auto-store keeps backward-compatible session:{sid} tag
- CLI store command accepts --trust-level, --session-id, --agent-role
- All write paths set source correctly (no erroneous 'default' for known paths)
- Full test suite green including provenance tests from Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-provenance-tagging/11.1-02-SUMMARY.md`
</output>
