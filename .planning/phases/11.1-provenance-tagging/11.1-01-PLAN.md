---
phase: 11.1-provenance-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/memcp-core/migrations/021_provenance_trust.sql
  - crates/memcp-core/src/storage/store/mod.rs
  - crates/memcp-core/src/storage/store/postgres.rs
  - crates/memcp-core/src/import/batch.rs
  - crates/memcp-core/tests/provenance_test.rs
  - crates/memcp-core/tests/source_audit_test.rs
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
  - PROV-05
  - PROV-06

must_haves:
  truths:
    - "Memories stored without explicit trust_level get system-inferred trust based on source/actor_type"
    - "Memories stored with explicit trust_level honor the caller's value"
    - "session_id and agent_role are stored and retrievable on memories"
    - "Trust level changes produce an audit trail in metadata.trust_history"
    - "List endpoint can filter by session_id and agent_role"
  artifacts:
    - path: "crates/memcp-core/migrations/021_provenance_trust.sql"
      provides: "trust_level, session_id, agent_role, metadata columns + indexes"
      contains: "ALTER TABLE memories ADD COLUMN"
    - path: "crates/memcp-core/src/storage/store/mod.rs"
      provides: "Updated Memory, CreateMemory, UpdateMemory, ListFilter structs with provenance fields"
      contains: "trust_level"
    - path: "crates/memcp-core/src/storage/store/postgres.rs"
      provides: "INSERT/SELECT/UPDATE queries with provenance columns, update_trust_level method"
      contains: "trust_history"
    - path: "crates/memcp-core/tests/provenance_test.rs"
      provides: "Integration tests for PROV-02 through PROV-06"
  key_links:
    - from: "crates/memcp-core/src/storage/store/mod.rs"
      to: "crates/memcp-core/src/storage/store/postgres.rs"
      via: "CreateMemory.trust_level Option<f32> resolved to f32 in store()"
      pattern: "infer_trust_level"
    - from: "crates/memcp-core/src/storage/store/postgres.rs"
      to: "migrations/021_provenance_trust.sql"
      via: "INSERT/SELECT columns match migration schema"
      pattern: "trust_level.*session_id.*agent_role.*metadata"
---

<objective>
Add provenance columns (trust_level, session_id, agent_role, metadata) to the memories table and propagate through core storage structs and postgres queries. This is the foundation — all write/read paths through the store layer get provenance support.

Purpose: Enables recording WHO wrote WHAT with WHAT authority on every memory, defending against memory poisoning (OWASP ASI06).
Output: Migration, updated structs, updated postgres queries, trust inference function, trust update with audit trail, integration tests.
</objective>

<execution_context>
@/Users/ayoamadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoamadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-provenance-tagging/11.1-CONTEXT.md
@.planning/phases/11.1-provenance-tagging/11.1-RESEARCH.md

@crates/memcp-core/src/storage/store/mod.rs
@crates/memcp-core/src/storage/store/postgres.rs
@crates/memcp-core/src/import/batch.rs
@crates/memcp-core/migrations/020_rename_workspace_to_project.sql

<interfaces>
<!-- Existing structs to extend (from store/mod.rs) -->

Memory struct (lines 18-70): 24 fields ending with `project: Option<String>`. Add trust_level, session_id, agent_role, metadata after project.

CreateMemory struct (lines 76-123): 15 fields ending with `project: Option<String>`. Add trust_level: Option<f32>, session_id: Option<String>, agent_role: Option<String> with #[serde(default)].

UpdateMemory struct (lines 145-154): Currently 4 optional fields. Add trust_level: Option<f32>.

ListFilter struct (lines 158-182): Has actor, audience, project filters. Add session_id: Option<String> and agent_role: Option<String>.

Default impls for ListFilter (lines 184-199): Add session_id: None, agent_role: None.

SearchFilter struct (lines 220-239): Do NOT add session_id here (per research anti-pattern — session filtering belongs on list/recall, not vector search).

<!-- Key functions in postgres.rs -->
store() at line 647: INSERT with ~19 columns, builds CreateMemory into INSERT query.
row_to_memory(): Maps SQL rows to Memory struct — must add all new columns.
update(): Builds dynamic SET clause from UpdateMemory fields.
Multiple SELECT queries across the file — ALL must include new columns.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + structs + trust inference function</name>
  <files>
    crates/memcp-core/migrations/021_provenance_trust.sql
    crates/memcp-core/src/storage/store/mod.rs
  </files>
  <action>
1. Create migration `021_provenance_trust.sql`:
   ```sql
   ALTER TABLE memories ADD COLUMN IF NOT EXISTS trust_level REAL NOT NULL DEFAULT 0.5;
   ALTER TABLE memories ADD COLUMN IF NOT EXISTS session_id TEXT;
   ALTER TABLE memories ADD COLUMN IF NOT EXISTS agent_role TEXT;
   ALTER TABLE memories ADD COLUMN IF NOT EXISTS metadata JSONB NOT NULL DEFAULT '{}'::jsonb;
   CREATE INDEX IF NOT EXISTS idx_memories_trust_level ON memories(trust_level);
   CREATE INDEX IF NOT EXISTS idx_memories_session_id ON memories(session_id) WHERE session_id IS NOT NULL;
   CREATE INDEX IF NOT EXISTS idx_memories_agent_role ON memories(agent_role) WHERE agent_role IS NOT NULL;
   ```

2. In `store/mod.rs`, add to `Memory` struct (after `project` field):
   - `pub trust_level: f32` (NOT optional — DB is NOT NULL DEFAULT 0.5)
   - `pub session_id: Option<String>`
   - `pub agent_role: Option<String>`
   - `pub metadata: serde_json::Value` (NOT optional — DB is NOT NULL DEFAULT '{}')

3. In `CreateMemory` struct, add (after `project` field):
   - `#[serde(default)] pub trust_level: Option<f32>` (None = system infers)
   - `#[serde(default)] pub session_id: Option<String>`
   - `#[serde(default)] pub agent_role: Option<String>`

4. In `UpdateMemory` struct, add:
   - `pub trust_level: Option<f32>`

5. In `ListFilter` struct, add:
   - `pub session_id: Option<String>`
   - `pub agent_role: Option<String>`
   Update the Default impl to include `session_id: None, agent_role: None`.

6. Add `infer_trust_level` function in mod.rs (public, used by postgres.rs):
   ```rust
   pub fn infer_trust_level(source: &str, actor_type: &str) -> f32 {
       if actor_type == "user" || source == "cli" { return 0.8; }
       match actor_type {
           "auto-store" => 0.3,
           _ => match source {
               "import" => 0.4,
               _ => 0.5,
           },
       }
   }
   ```

7. Fix ALL existing CreateMemory construction sites that will fail to compile (search codebase for `CreateMemory {`). Each needs the three new fields added. For most, use `trust_level: None, session_id: None, agent_role: None`. Check: auto_store/mod.rs, import/batch.rs, tests, benchmark code, stress tests, load test corpus.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo check -p memcp-core 2>&1 | tail -20</automated>
  </verify>
  <done>Migration file exists. Memory/CreateMemory/UpdateMemory/ListFilter structs compile with new fields. infer_trust_level function exists and is public. All CreateMemory construction sites updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Postgres queries + trust update method + tests</name>
  <files>
    crates/memcp-core/src/storage/store/postgres.rs
    crates/memcp-core/src/import/batch.rs
    crates/memcp-core/tests/provenance_test.rs
    crates/memcp-core/tests/source_audit_test.rs
  </files>
  <behavior>
    - PROV-01: After migration, store a memory → trust_level, session_id, agent_role, metadata columns exist and readable
    - PROV-02: Store without trust_level, source="cli" → trust_level=0.8; actor_type="auto-store" → 0.3; source="import" → 0.4; default → 0.5
    - PROV-03: Store with explicit trust_level=0.9 → stored as 0.9 (not overridden by inference)
    - PROV-04: Store with session_id="sess-123" → get returns session_id="sess-123"; list with session_id filter returns only matching memories
    - PROV-05: Store with agent_role="coder" → get returns agent_role="coder"; list with agent_role filter returns only matching memories
    - PROV-06: update_trust_level(id, 0.3, "suspicious_pattern") → trust_level becomes 0.3, metadata.trust_history contains entry {from: 0.5, to: 0.3, reason: "suspicious_pattern", at: <timestamp>}
  </behavior>
  <action>
1. In `postgres.rs`, update `row_to_memory()`:
   Add `trust_level: row.try_get("trust_level").unwrap_or(0.5)`, `session_id: row.try_get("session_id").unwrap_or(None)`, `agent_role: row.try_get("agent_role").unwrap_or(None)`, `metadata: row.try_get("metadata").unwrap_or_else(|_| serde_json::json!({}))`.

2. Update the `store()` method INSERT query:
   - Add trust_level, session_id, agent_role, metadata to column list
   - For trust_level bind: `input.trust_level.unwrap_or_else(|| infer_trust_level(&input.source, &input.actor_type))`
   - For session_id: bind `input.session_id.as_deref()`
   - For agent_role: bind `input.agent_role.as_deref()`
   - For metadata: bind `serde_json::json!({})`

3. Update ALL SELECT queries that feed into `row_to_memory()`. CRITICAL: grep for every SELECT in postgres.rs that reads from memories table. Each must include `trust_level, session_id, agent_role, metadata` in its column list. There are 6+ such queries (store idempotency, content-hash dedup, get, list, search, get_pending_memories, get_unenriched_memories, consolidation queries).

4. Update `update()` method: add trust_level to the dynamic SET clause builder (same pattern as existing optional fields). When trust_level changes via update(), also append to metadata.trust_history. Read current trust_level and metadata first, then do atomic update.

5. Add dedicated `update_trust_level(&self, id: &str, new_trust: f32, reason: &str) -> Result<(), MemcpError>` method:
   - Single UPDATE using `jsonb_set(metadata, '{trust_history}', COALESCE(metadata->'trust_history', '[]'::jsonb) || $3::jsonb)`
   - The appended JSON entry: `{"from": <old>, "to": <new>, "reason": "<reason>", "at": "<now>"}`
   - Must read current trust_level first (SELECT trust_level FROM memories WHERE id = $1)

6. Update list query builder: when `filter.session_id` is Some, add `AND session_id = $N`; when `filter.agent_role` is Some, add `AND agent_role = $N`.

7. Update `import/batch.rs` INSERT query: add trust_level (0.4 for imports), session_id (NULL), agent_role (NULL), metadata ('{}') columns.

8. Create `tests/provenance_test.rs` with tests for PROV-02 through PROV-06 (use common test helpers, DATABASE_URL connection pattern from existing tests like store_test.rs).

9. Create `tests/source_audit_test.rs` with test for PROV-10: verify that store via each path (direct store, import) sets source correctly (not 'default' unless truly default).
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && DATABASE_URL=postgres://memcp:memcp@localhost:5433/memcp cargo test -p memcp-core provenance -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <done>All postgres queries include provenance columns. Trust inference works for all source/actor_type combinations. Trust update produces audit trail. List filtering by session_id and agent_role works. Import batch query includes provenance columns. provenance_test.rs and source_audit_test.rs pass.</done>
</task>

</tasks>

<verification>
```bash
# Full test suite including new provenance tests
DATABASE_URL=postgres://memcp:memcp@localhost:5433/memcp cargo test -p memcp-core -- --test-threads=1
```
</verification>

<success_criteria>
- Migration 021 adds trust_level, session_id, agent_role, metadata columns with correct types/defaults/indexes
- Memory, CreateMemory, UpdateMemory, ListFilter structs have new fields
- infer_trust_level correctly maps source/actor_type pairs to trust values per CONTEXT.md
- All SELECT queries in postgres.rs include new columns (no runtime row_to_memory failures)
- update_trust_level produces trust_history audit entries in metadata JSONB
- ListFilter session_id and agent_role filters work in list queries
- Import batch.rs sets trust_level=0.4 for imported memories
- All existing tests pass + new provenance tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-provenance-tagging/11.1-01-SUMMARY.md`
</output>
