---
phase: 11.1-provenance-tagging
plan: 02
subsystem: api
tags: [mcp, http-api, cli, provenance, trust-level, session-id, auto-store]

requires:
  - phase: 11.1-01
    provides: "CreateMemory/UpdateMemory/ListFilter with trust_level, session_id, agent_role fields; infer_trust_level; migration 021"
provides:
  - "MCP store_memory tool accepts trust_level, session_id, agent_role params"
  - "HTTP API store/update endpoints accept provenance fields"
  - "Auto-store sets trust_level=0.3 and populates session_id column"
  - "CLI store command accepts --trust-level, --session-id, --agent-role flags"
  - "All write paths use correct source identifiers (cli, mcp, api, auto-store)"
affects: [11.1-03, 12-auth]

tech-stack:
  added: []
  patterns: ["provenance field propagation through transport layers"]

key-files:
  created: []
  modified:
    - "crates/memcp-core/src/transport/server.rs"
    - "crates/memcp-core/src/transport/api/types.rs"
    - "crates/memcp-core/src/transport/api/store.rs"
    - "crates/memcp-core/src/transport/api/update.rs"
    - "crates/memcp-core/src/pipeline/auto_store/mod.rs"
    - "crates/memcp-core/src/cli.rs"
    - "crates/memcp/src/main.rs"

key-decisions:
  - "MCP source default changed from 'default' to 'mcp' for provenance correctness"
  - "CLI source default changed from 'default' to 'cli' for provenance correctness"
  - "Auto-store trust_level=0.3 applied to both parent and chunk memories"

patterns-established:
  - "Provenance fields included in CLI JSON output only when non-null/non-default (token-saving for agents)"

requirements-completed: [PROV-07, PROV-08, PROV-09, PROV-10]

duration: 6min
completed: 2026-03-09
---

# Phase 11.1 Plan 02: Transport Layer Provenance Summary

**End-to-end provenance field propagation through MCP tools, HTTP API, CLI, and auto-store with source audit fixes**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-09T03:13:03Z
- **Completed:** 2026-03-09T03:19:25Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- MCP store_memory tool schema includes trust_level, session_id, agent_role with agent-friendly descriptions
- HTTP API store and update endpoints accept provenance fields end-to-end
- Auto-store populates session_id column from parsed log entry AND keeps backward-compatible session:{sid} tag
- Auto-store sets trust_level=0.3 for both parent and chunk memories
- CLI store command accepts --trust-level, --session-id, --agent-role flags
- Source audit: fixed MCP default from "default" to "mcp", CLI default from "default" to "cli"

## Task Commits

Each task was committed atomically:

1. **Task 1: MCP + HTTP API transport layer provenance fields** - `7c1f8e3` (feat)
2. **Task 2: Auto-store session_id promotion + CLI flags + source audit** - `47a8b57` (feat)

## Files Created/Modified
- `crates/memcp-core/src/transport/server.rs` - Added trust_level/session_id/agent_role to StoreMemoryParams, UpdateMemoryParams, ListMemoriesParams; wired to CreateMemory/UpdateMemory/ListFilter; updated tool description; fixed MCP source default
- `crates/memcp-core/src/transport/api/types.rs` - Added provenance fields to StoreRequest and UpdateRequest
- `crates/memcp-core/src/transport/api/store.rs` - Wired trust_level/session_id/agent_role from StoreRequest to CreateMemory
- `crates/memcp-core/src/transport/api/update.rs` - Wired trust_level from UpdateRequest to UpdateMemory; updated validation
- `crates/memcp-core/src/pipeline/auto_store/mod.rs` - Set trust_level=0.3, session_id from entry on both parent and chunk CreateMemory sites
- `crates/memcp-core/src/cli.rs` - Added trust_level/session_id/agent_role params to cmd_store; updated format_memory_json for provenance output
- `crates/memcp/src/main.rs` - Added --trust-level/--session-id/--agent-role CLI args; fixed source default to "cli"

## Decisions Made
- MCP source default changed from "default" to "mcp" -- agents using MCP should be identifiable in provenance
- CLI source default changed from "default" to "cli" -- consistent with infer_trust_level mapping (cli -> 0.8)
- Auto-store chunks inherit parent's trust_level=0.3 and session_id for consistent provenance

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed MCP source default from "default" to "mcp"**
- **Found during:** Task 2 (source audit)
- **Issue:** MCP store_memory defaulted source to "default", making it indistinguishable from other paths
- **Fix:** Changed default to "mcp" so infer_trust_level can correctly map it
- **Files modified:** crates/memcp-core/src/transport/server.rs
- **Committed in:** 47a8b57

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Source audit fix was required by PROV-10. No scope creep.

## Issues Encountered
- Pre-existing test failure in benchmark::locomo::dataset::tests::test_load_locomo_dataset_valid (unrelated to provenance changes, dataset parsing issue)

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All transport layers now carry provenance metadata end-to-end
- Ready for Plan 03 (provenance-aware search/recall filtering) if planned
- trust_level inference works correctly with new source defaults (mcp->0.7, cli->0.8, api->0.5, auto-store->0.3)

---
*Phase: 11.1-provenance-tagging*
*Completed: 2026-03-09*
