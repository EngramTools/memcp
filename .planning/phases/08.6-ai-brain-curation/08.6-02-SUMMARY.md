---
phase: 08.6-ai-brain-curation
plan: 02
subsystem: pipeline
tags: [ollama, openai, llm, curation, reqwest]

requires:
  - phase: 08.6-ai-brain-curation-01
    provides: CurationProvider trait, CurationAction enum, ClusterMember struct, CurationConfig
provides:
  - OllamaCurationProvider with structured JSON review prompts
  - OpenAICurationProvider with OpenAI-compatible HTTP pattern
  - Factory function wiring both providers from CurationConfig
affects: [08.6-ai-brain-curation-03, 08.6-ai-brain-curation-04]

tech-stack:
  added: []
  patterns: [shared-prompt-constants, fail-open-json-parsing, reuse-helpers-across-providers]

key-files:
  created:
    - crates/memcp-core/src/pipeline/curation/ollama.rs
    - crates/memcp-core/src/pipeline/curation/openai.rs
  modified:
    - crates/memcp-core/src/pipeline/curation/mod.rs

key-decisions:
  - "Shared prompt constants and helpers in ollama.rs, reused by openai.rs via pub(crate)"
  - "Fail-open JSON parsing: unparseable LLM response falls back to Skip actions, not errors"
  - "Markdown code fence stripping before JSON parse to handle LLMs wrapping in ```json"

patterns-established:
  - "Curation providers share format_cluster() and parse_review_response() from ollama module"
  - "10-second timeout for cluster review LLM calls"

requirements-completed: []

duration: 4min
completed: 2026-03-06
---

# Phase 08.6 Plan 02: LLM Curation Providers Summary

**Ollama and OpenAI curation providers with structured JSON review prompts, fail-open parsing, and factory wiring**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-06T04:27:42Z
- **Completed:** 2026-03-06T04:31:40Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- OllamaCurationProvider with review_cluster and synthesize_merge via /api/chat endpoint
- OpenAICurationProvider with same logic via /chat/completions with Bearer auth
- Shared prompt constants (REVIEW_SYSTEM_PROMPT, MERGE_SYSTEM_PROMPT) and helpers (format_cluster, parse_review_response)
- Factory function in mod.rs creates correct provider based on CurationConfig.llm_provider
- Fail-open JSON parsing: strips markdown fences, falls back to Skip on parse failure

## Task Commits

Implementation was committed in Plan 01's execution (providers were built alongside the foundation):

1. **Task 1: Implement OllamaCurationProvider** - `18343bb` (feat: curation foundation)
2. **Task 2: Implement OpenAICurationProvider and wire factory** - `18343bb` (feat: curation foundation)

Additional fix during verification:
- **Benchmark test fix** - `74a63b1` (fix: add missing adversarial_answer field)

## Files Created/Modified
- `crates/memcp-core/src/pipeline/curation/ollama.rs` - Ollama curation provider with chat(), format_cluster(), parse_review_response(), prompt constants
- `crates/memcp-core/src/pipeline/curation/openai.rs` - OpenAI curation provider reusing ollama helpers
- `crates/memcp-core/src/pipeline/curation/mod.rs` - Factory function create_curation_provider()
- `crates/memcp-core/src/benchmark/locomo/mod.rs` - Fixed missing adversarial_answer field in tests

## Decisions Made
- Shared prompt constants and helpers live in ollama.rs with pub(crate) visibility, reused by openai.rs
- Fail-open parsing: bad LLM JSON response produces Skip actions for all cluster members rather than erroring
- Markdown code fence stripping handles LLMs that wrap JSON in ```json blocks

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed missing adversarial_answer field in benchmark tests**
- **Found during:** Verification (cargo test)
- **Issue:** QaPair struct had adversarial_answer field added but test initializers didn't include it, blocking test compilation
- **Fix:** Added `adversarial_answer: None` to both test QaPair initializers
- **Files modified:** crates/memcp-core/src/benchmark/locomo/mod.rs
- **Verification:** cargo test --lib passes (148 tests), cargo test --tests passes (71 tests)
- **Committed in:** 74a63b1

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Pre-existing test compilation issue, not caused by curation work. Minimal fix.

## Issues Encountered
- Plan 02 work was already completed during Plan 01 execution (both LLM providers were built as part of the curation foundation commit). Verified all implementations match plan specifications.
- Pre-existing doctest failure in transport/api/mod.rs (references jwt_middleware not in scope) -- out of scope, logged.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both LLM curation providers ready for integration in Plan 03 (curation worker pipeline)
- Factory function tested and operational
- All 148 lib tests + 71 integration tests passing

---
*Phase: 08.6-ai-brain-curation*
*Completed: 2026-03-06*
