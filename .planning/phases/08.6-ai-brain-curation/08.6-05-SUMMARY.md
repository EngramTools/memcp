---
phase: 08.6-ai-brain-curation
plan: 05
subsystem: pipeline
tags: [curation, dry-run, cli, rust]

requires:
  - phase: 08.6-03
    provides: curation worker pipeline with run_curation()
  - phase: 08.6-04
    provides: CLI curation commands including --propose flag
provides:
  - "True dry-run mode for curation --propose (no DB side effects)"
  - "CurationResult.proposed_actions field for action preview"
affects: [curation, cli]

tech-stack:
  added: []
  patterns: ["dry_run bool parameter gates all store mutations in worker"]

key-files:
  created: []
  modified:
    - crates/memcp-core/src/pipeline/curation/worker.rs
    - crates/memcp-core/src/pipeline/curation/mod.rs
    - crates/memcp-core/src/cli.rs
    - crates/memcp-core/src/transport/daemon.rs

key-decisions:
  - "dry_run still calls provider.review_cluster() and synthesize_merge() (read-only LLM calls) to show realistic proposals"
  - "Placeholder run_id 'dry-run' used instead of creating a DB record"
  - "Pretty-printed JSON for --propose output (human review), compact for normal runs"

patterns-established:
  - "dry_run parameter pattern: gates DB mutations while allowing read-only provider calls"

requirements-completed: []

duration: 4min
completed: 2026-03-06
---

# Phase 08.6 Plan 05: Curation Dry-Run (--propose) Summary

**True dry-run mode for `memcp curation run --propose` that previews actions without any database mutations**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-06T04:48:20Z
- **Completed:** 2026-03-06T04:52:08Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `proposed_actions: Vec<CurationAction>` to CurationResult for collecting dry-run proposals
- Implemented dry_run code path in worker that skips all DB writes (no run record, no merges, no tags, no stability changes)
- Wired --propose CLI flag to dry_run=true with "status: proposed" pretty-printed JSON output
- Removed TODO placeholder and warning messages

## Task Commits

Each task was committed atomically:

1. **Task 1: Add dry_run mode to curation worker and result type** - `10f6fb4` (feat)
2. **Task 2: Wire --propose flag to dry_run mode in CLI and display proposals** - `32fb0ad` (feat)

## Files Created/Modified
- `crates/memcp-core/src/pipeline/curation/mod.rs` - Added proposed_actions field to CurationResult
- `crates/memcp-core/src/pipeline/curation/worker.rs` - Added dry_run parameter, gates all store calls
- `crates/memcp-core/src/cli.rs` - Wired propose->dry_run, added proposed output format
- `crates/memcp-core/src/transport/daemon.rs` - Updated call site with dry_run: false

## Decisions Made
- dry_run still calls LLM provider for review/synthesis (read-only) to show realistic proposals
- Used "dry-run" as placeholder run_id rather than empty string for clarity
- Pretty-printed JSON for propose output since it's meant for human review

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Curation --propose now fully functional for safe previewing
- All 5 plans in phase 08.6 complete

---
*Phase: 08.6-ai-brain-curation*
*Completed: 2026-03-06*
