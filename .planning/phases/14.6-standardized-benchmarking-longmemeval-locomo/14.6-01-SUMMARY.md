---
phase: 14.6-standardized-benchmarking-longmemeval-locomo
plan: 01
subsystem: testing
tags: [benchmark, locomo, f1-scoring, jsonl, squad-style]

# Dependency graph
requires:
  - phase: 06.3-benchmarking
    provides: Existing LongMemEval benchmark harness (dataset.rs, runner.rs, evaluate.rs, report.rs, ingest.rs)
provides:
  - benchmark/locomo/ module with typed structs, dataset loader, F1 scorer, dual-mode ingestion
  - HistoryEntry + append_history() for JSONL benchmark score tracking
  - JUDGE_MODEL switched to gpt-4o-mini for ~40% cost reduction
affects:
  - 14.6-02 (Plan 02 wires locomo module into runner and CLI)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - SQuAD-style token-level F1 scoring (normalize → tokenize → multiset intersection → precision/recall)
    - Flexible category deserialization via serde_json::Value with category_u8() normalizer
    - JSONL append-only writes using OpenOptions::append(true) for benchmark history
    - Per-turn timestamp offsets (1s per turn) for session ordering in temporal benchmarks

key-files:
  created:
    - crates/memcp-core/src/benchmark/locomo/mod.rs
    - crates/memcp-core/src/benchmark/locomo/dataset.rs
    - crates/memcp-core/src/benchmark/locomo/evaluate.rs
    - crates/memcp-core/src/benchmark/locomo/ingest.rs
  modified:
    - crates/memcp-core/src/benchmark/mod.rs
    - crates/memcp-core/src/benchmark/evaluate.rs
    - crates/memcp-core/src/benchmark/report.rs

key-decisions:
  - "F1 partial overlap test corrected: 'The capital is Paris' vs 'Paris' yields 0.4 (not > 0.5) — SQuAD math is correct; test updated to use 'Paris France' vs 'Paris' = 0.667 for > 0.5 assertion"
  - "call_openai_with_retry, ANSWER_MODEL, OPENAI_CHAT_URL made pub(crate) so locomo/evaluate.rs can reuse LongMemEval retry logic without duplication"
  - "JUDGE_MODEL switched to openai/gpt-4o-mini-2024-07-18 per locked CONTEXT.md decision; ANSWER_MODEL stays openai/gpt-4o-2024-08-06"
  - "HistoryEntry uses JSONL format (not JSON array) so append never requires reading the full file"
  - "parse_locomo_date handles 3 formats: ISO 8601, long month ('March 15, 2023'), and US slash (03/15/2023)"

patterns-established:
  - "Module-level doc comment style: use /// with no blank line before first use/pub declaration (matches existing benchmark files)"
  - "LoCoMo ingestion: PerTurn creates one memory per turn with 1s offset; PerSession concatenates full session with date prefix line"

requirements-completed: [BENCH-01, BENCH-02, BENCH-03, BENCH-05, BENCH-06]

# Metrics
duration: 17min
completed: 2026-03-04
---

# Phase 14.6 Plan 01: LoCoMo Benchmark Foundation Summary

**SQuAD-style F1 scorer, dual-mode LoCoMo ingestion (per-turn/per-session), and JSONL benchmark history append with gpt-4o-mini judge switch**

## Performance

- **Duration:** 17 min
- **Started:** 2026-03-04T01:44:11Z
- **Completed:** 2026-03-04T02:01:31Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Created `benchmark/locomo/` module with all foundation types (LoCoMoSample, Session, Turn, QaPair with flexible category deserialization)
- Implemented SQuAD-style token-level F1 scorer with punctuation normalization and multiset intersection
- Dual-mode ingestion (PerTurn: 1 memory/turn with 1s offsets, PerSession: concatenated session content)
- `append_history()` JSONL write for benchmark score tracking — append-only, no full-file rewrite
- Switched JUDGE_MODEL to gpt-4o-mini-2024-07-18 for ~40% cost reduction while keeping ANSWER_MODEL at GPT-4o
- Made `call_openai_with_retry`, `ANSWER_MODEL`, `OPENAI_CHAT_URL` pub(crate) for locomo reuse
- 23 unit tests passing: F1 edge cases, category deserialization, date parsing, dataset loading, history append

## Task Commits

Each task was committed atomically:

1. **Task 1: LoCoMo types, dataset loader, and F1 scorer (TDD)** - `9d2ef2d` (feat)
2. **Task 2: LoCoMo ingestion, history append, judge model switch** - `fd23eb6` (feat)
3. **Fix: clippy doc comment formatting in locomo module** - `475ae56` (fix)

## Files Created/Modified
- `crates/memcp-core/src/benchmark/locomo/mod.rs` - LoCoMoSample, Session, Turn, QaPair, LoCoMoIngestionMode, LoCoMoQuestionResult, LoCoMoResult types; category_label() helper
- `crates/memcp-core/src/benchmark/locomo/dataset.rs` - load_locomo_dataset() JSON array parser
- `crates/memcp-core/src/benchmark/locomo/evaluate.rs` - f1_score(), normalize_and_tokenize(), token_counts(), generate_locomo_answer()
- `crates/memcp-core/src/benchmark/locomo/ingest.rs` - ingest_sample() with PerTurn/PerSession modes; parse_locomo_date() multi-format parser
- `crates/memcp-core/src/benchmark/mod.rs` - Added `pub mod locomo;` declaration
- `crates/memcp-core/src/benchmark/evaluate.rs` - Made call_openai_with_retry/ANSWER_MODEL/OPENAI_CHAT_URL pub(crate); switched JUDGE_MODEL to gpt-4o-mini
- `crates/memcp-core/src/benchmark/report.rs` - Added HistoryEntry struct and append_history() JSONL utility

## Decisions Made
- **F1 test expectation corrected:** Plan spec said `f1_score("The capital is Paris", "Paris") > 0.5` but SQuAD math gives 0.4 (precision=0.25, recall=1.0). Test updated to assert `> 0.0` for that case and added `f1_score("Paris France", "Paris") > 0.5` as the `> 0.5` assertion example.
- **pub(crate) not pub:** Exposed retry utilities as pub(crate) instead of pub — needed only within the benchmark module family, not the public API.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed incorrect F1 test expectation**
- **Found during:** Task 1 (TDD RED phase)
- **Issue:** Plan specified `f1_score("The capital is Paris", "Paris") > 0.5` but the correct SQuAD F1 for this input is 0.4 (precision=1/4, recall=1/1, F1=0.4)
- **Fix:** Updated test to assert `> 0.0` for partial overlap case and added a `> 0.5` example using "Paris France" vs "Paris" (F1=2/3)
- **Files modified:** `crates/memcp-core/src/benchmark/locomo/evaluate.rs`
- **Verification:** All 21 locomo tests pass after fix
- **Committed in:** 9d2ef2d (Task 1 commit)

**2. [Rule 1 - Bug] Fixed clippy doc comment formatting**
- **Found during:** Post-task verification (clippy -D warnings)
- **Issue:** New locomo files had empty blank line between `///` doc comment block and first item, triggering `empty_line_after_doc_comment` clippy lint
- **Fix:** Removed blank lines after `///` blocks in all 4 locomo module files
- **Files modified:** `locomo/{mod,dataset,evaluate,ingest}.rs`
- **Verification:** `cargo clippy` shows no errors from locomo module
- **Committed in:** 475ae56

---

**Total deviations:** 2 auto-fixed (both Rule 1 bugs)
**Impact on plan:** Both fixes necessary for correctness and code quality. No scope creep.

## Issues Encountered
- Pre-existing clippy errors in `import/markdown.rs`, `import/chatgpt.rs`, `benchmark/report.rs` (print_comparison function) — these are out-of-scope and not fixed. Documented for deferred cleanup.

## Next Phase Readiness
- All foundation types and utilities for Plan 02 are in place
- Plan 02 can wire `locomo/ingest.rs`, `locomo/evaluate.rs` into a runner module and add `--benchmark locomo` CLI dispatch
- `HistoryEntry` and `append_history()` ready for both LongMemEval and LoCoMo runners to call

---
*Phase: 14.6-standardized-benchmarking-longmemeval-locomo*
*Completed: 2026-03-04*
