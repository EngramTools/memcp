---
phase: 21-pii-and-secret-redaction
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - crates/memcp-core/src/transport/api/store.rs
  - crates/memcp-core/src/transport/api/types.rs
  - crates/memcp-core/src/transport/server.rs
  - crates/memcp-core/src/transport/daemon.rs
  - crates/memcp-core/src/pipeline/auto_store/mod.rs
  - crates/memcp/src/main.rs
  - crates/memcp-core/tests/redaction_integration.rs
autonomous: true
requirements:
  - RED-08
  - RED-09
  - RED-10
  - RED-11
  - RED-12
  - RED-13

must_haves:
  truths:
    - "All four ingestion paths (CLI, HTTP API, MCP, auto-store) redact secrets before storing"
    - "CLI --no-redact flag bypasses redaction"
    - "HTTP API and MCP skip_redaction parameter bypasses redaction"
    - "Auto-store sidecar always redacts, no bypass possible"
    - "Redaction happens before content filter, before embedding generation"
    - "Fail-closed: if redaction errors, store is rejected with category-only error message"
    - "tracing::warn emitted on redaction with category + count (no secret values in logs)"
    - "Store response includes redaction metadata (count + categories) when redactions occurred"
  artifacts:
    - path: "crates/memcp-core/src/transport/api/store.rs"
      provides: "HTTP API store_handler with redaction before store"
      contains: "redact"
    - path: "crates/memcp-core/src/transport/server.rs"
      provides: "MCP store_memory with redaction + skip_redaction param"
      contains: "redact"
    - path: "crates/memcp-core/src/pipeline/auto_store/mod.rs"
      provides: "Auto-store worker with mandatory redaction (no bypass)"
      contains: "redact"
    - path: "crates/memcp-core/tests/redaction_integration.rs"
      provides: "Integration tests for all ingestion paths"
      min_lines: 80
  key_links:
    - from: "crates/memcp-core/src/transport/api/store.rs"
      to: "crates/memcp-core/src/pipeline/redaction/mod.rs"
      via: "RedactionEngine::redact() called in store_handler"
      pattern: "redaction_engine.*redact"
    - from: "crates/memcp-core/src/transport/server.rs"
      to: "crates/memcp-core/src/pipeline/redaction/mod.rs"
      via: "RedactionEngine::redact() called in store_memory"
      pattern: "redaction_engine.*redact"
    - from: "crates/memcp-core/src/pipeline/auto_store/mod.rs"
      to: "crates/memcp-core/src/pipeline/redaction/mod.rs"
      via: "RedactionEngine::redact() called before content filter"
      pattern: "redaction_engine.*redact"
    - from: "crates/memcp-core/src/transport/daemon.rs"
      to: "crates/memcp-core/src/pipeline/redaction/mod.rs"
      via: "RedactionEngine constructed at daemon startup, passed to workers"
      pattern: "RedactionEngine::from_config"
---

<objective>
Wire RedactionEngine into all four ingestion paths (CLI, HTTP API, MCP server, auto-store), add bypass flags (--no-redact for CLI, skip_redaction for API/MCP, no bypass for auto-store), add redaction metadata to store responses, and create integration tests.

Purpose: Without wiring, the engine is inert. This plan closes the gap where CLI and HTTP API have NO content filtering at all, and ensures secrets are caught on every ingestion path.
Output: Redaction active on all store operations, bypass controls, integration tests proving end-to-end behavior.
</objective>

<execution_context>
@/Users/ayoamadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoamadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-pii-and-secret-redaction/21-CONTEXT.md
@.planning/phases/21-pii-and-secret-redaction/21-RESEARCH.md
@.planning/phases/21-pii-and-secret-redaction/21-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (RedactionEngine API) -->
From crates/memcp-core/src/pipeline/redaction/mod.rs:
```rust
pub struct RedactionEngine { ... }
impl RedactionEngine {
    pub fn from_config(config: &RedactionConfig) -> Result<Self, MemcpError>;
    pub fn redact(&self, content: &str) -> Result<RedactionResult, MemcpError>;
}

pub struct RedactionResult {
    pub content: String,
    pub redaction_count: usize,
    pub categories: Vec<String>,
    pub was_redacted: bool,
}
```

From crates/memcp-core/src/config.rs:
```rust
pub struct RedactionConfig {
    pub secrets_enabled: bool,   // default: true
    pub pii_enabled: bool,       // default: false
    pub entropy_threshold: f64,  // default: 3.5
    pub allowlist: AllowlistConfig,
    pub custom_rules: Vec<CustomRuleConfig>,
}
```

<!-- Existing integration points to modify -->
From crates/memcp-core/src/transport/api/store.rs:
```rust
pub async fn store_handler(...) -> impl IntoResponse { ... }
```

From crates/memcp-core/src/transport/server.rs:
```rust
// MCP store_memory tool handler
```

From crates/memcp-core/src/pipeline/auto_store/mod.rs:
```rust
// Auto-store worker processes JSONL entries
```

From crates/memcp-core/src/transport/daemon.rs:
```rust
// Daemon startup constructs shared state and spawns workers
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Daemon construction + HTTP API + MCP wiring</name>
  <files>
    crates/memcp-core/src/transport/daemon.rs,
    crates/memcp-core/src/transport/api/store.rs,
    crates/memcp-core/src/transport/api/types.rs,
    crates/memcp-core/src/transport/server.rs
  </files>
  <action>
    1. **Daemon startup** (`transport/daemon.rs`):
       - Construct `RedactionEngine::from_config(&config.redaction)` at daemon startup.
       - Wrap in `Arc<RedactionEngine>` and add to shared state (AppState or equivalent).
       - Pass to auto-store worker and MCP server context.
       - If construction fails and secrets_enabled=true, log error and exit (fail-closed for default-on feature). If secrets_enabled=false and pii_enabled=false, skip construction (redaction disabled entirely).

    2. **HTTP API** (`transport/api/store.rs`):
       - Add `skip_redaction: Option<bool>` to store request body (in types.rs if StoreRequest is there).
       - In `store_handler`, before constructing CreateMemory:
         - If `skip_redaction` is not true AND redaction engine is Some:
           - Call `engine.redact(&content)`.
           - On error: return 400 with category-only message ("Store rejected: redaction error").
           - On success with was_redacted: `tracing::warn!(categories = ?result.categories, count = result.redaction_count, "Content redacted")`.
           - Replace content with redacted content.
       - Add redaction metadata to store response JSON: `"redactions": { "count": N, "categories": [...] }` when was_redacted=true. Omit field entirely when no redactions.

    3. **MCP server** (`transport/server.rs`):
       - Add `skip_redaction` parameter to `store_memory` MCP tool schema.
       - In the store_memory handler, apply same redaction logic as HTTP API.
       - Redaction runs BEFORE the existing content_filter check.
       - Include redaction info in the MCP tool response content (as text, since MCP returns TextContent).

    4. **Store response type** (`transport/api/types.rs`):
       - Add `redactions: Option<RedactionInfo>` to store response struct.
       - `pub struct RedactionInfo { pub count: usize, pub categories: Vec<String> }` with `#[serde(skip_serializing_if = "Option::is_none")]` on the parent field.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo build -p memcp-core</automated>
  </verify>
  <done>
    HTTP API store_handler applies redaction before store with skip_redaction bypass. MCP store_memory applies redaction before content_filter with skip_redaction param. Daemon constructs RedactionEngine at startup. Store response includes redaction metadata when content was redacted.
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI + auto-store wiring + integration tests</name>
  <files>
    crates/memcp/src/main.rs,
    crates/memcp-core/src/pipeline/auto_store/mod.rs,
    crates/memcp-core/tests/redaction_integration.rs
  </files>
  <action>
    1. **CLI** (`crates/memcp/src/main.rs`):
       - Add `--no-redact` flag to the `store` subcommand (clap boolean flag).
       - In `cmd_store` (or equivalent store command handler):
         - If `--no-redact` is NOT set, construct `RedactionEngine::from_config(&config.redaction)` and call `engine.redact(&content)`.
         - On error: print error message with categories only, exit with non-zero code.
         - On success with was_redacted: print warning to stderr ("Warning: N items redacted (categories: ...)") and use redacted content for store.
         - CLI is direct Postgres, not going through daemon — engine constructed per-invocation (cheap, patterns compiled once via LazyLock in rules.rs if needed, or just construct fresh — it's fast enough for CLI).

    2. **Auto-store worker** (`pipeline/auto_store/mod.rs`):
       - Accept `Option<Arc<RedactionEngine>>` in worker constructor (passed from daemon).
       - In the ingestion pipeline, BEFORE content_filter check:
         - If engine is Some, call `engine.redact(&content)`.
         - On error: skip this memory with `tracing::error!` (fail-closed — don't store potentially secret content). Continue processing next entry.
         - On success with was_redacted: `tracing::warn!` with category + count.
         - Replace content with redacted version before passing to content_filter → categorization → summarization → store.
       - **No bypass mechanism**: auto-store sidecar always redacts (per user decision). The `skip_redaction` parameter does NOT exist for auto-store.

    3. **Integration tests** (`tests/redaction_integration.rs`):
       - Test: store via RedactionEngine with a secret → content stored has `[REDACTED:...]` marker, not the original secret.
       - Test: store with skip_redaction behavior (engine not applied) → content stored as-is.
       - Test: multiple secrets in one memory → all redacted, result.redaction_count matches.
       - Test: PII disabled by default → SSN passes through unredacted.
       - Test: PII enabled → SSN gets redacted.
       - Test: content with no secrets → RedactionResult.was_redacted is false, content unchanged.
       - Test: fail-closed — construct engine with invalid custom_rules regex → from_config returns Err.
       - Test: auto-store path always redacts (simulate auto-store worker calling redact without skip option).
       - Use `RedactionEngine::from_config()` directly in tests (no need for full daemon startup). These are unit-integration tests of the engine behavior, not full E2E through HTTP/MCP.
  </action>
  <verify>
    <automated>cd /Users/ayoamadi/projects/memcp && cargo test -p memcp-core redaction_integration -- --test-threads=1 && cargo build -p memcp</automated>
  </verify>
  <done>
    CLI store applies redaction with --no-redact bypass. Auto-store worker always redacts with no bypass. Integration tests prove: secrets are redacted in stored content, PII is opt-in only, fail-closed on error, skip_redaction bypasses when set, multiple redactions work correctly. Full binary builds without errors.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p memcp-core redaction -- --test-threads=1` — all unit + integration redaction tests pass
- `cargo test -p memcp-core entropy -- --test-threads=1` — entropy tests pass
- `cargo build -p memcp` — full binary compiles
- `cargo clippy -p memcp-core -- -D warnings` — no warnings
- `DATABASE_URL=postgres://memcp:memcp@localhost:5433/memcp cargo test -- --test-threads=1` — full suite still passes (no regressions)
</verification>

<success_criteria>
- All four ingestion paths (CLI, HTTP, MCP, auto-store) apply redaction before store
- CLI --no-redact flag bypasses redaction
- HTTP/MCP skip_redaction parameter bypasses redaction
- Auto-store has no bypass mechanism (always redacts)
- Redaction occurs before content_filter and before embedding generation
- tracing::warn emitted with category + count (no secret values)
- Store response includes redaction metadata when content was redacted
- Fail-closed: redaction errors reject the store operation
- Integration tests prove all behaviors
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-pii-and-secret-redaction/21-02-SUMMARY.md`
</output>
